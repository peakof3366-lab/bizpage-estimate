# 오버나이트 작업 로그 (2026-07-04 밤)

### [사전 준비] 22:29
- OVERNIGHT_PLAN.md 확인, git 상태 확인(clean, baseline 커밋 존재), 베이스라인 verify_render 실행해
  "정상 상태에서도 뜨는 8건의 net::ERR_NAME_NOT_RESOLVED"가 CDN/폰트 오프라인 오류임을 재확인.
- ai-loop/logs/ 가 git 추적되고 있어 검증할 때마다 커밋이 무거워지는 문제 발견 → .gitignore에
  `ai-loop/logs/`, `ai-loop/tmp_prompt.txt` 추가하고 기존 추적 로그 git rm --cached 처리(파일은
  디스크에 그대로 있음, 삭제 아님). 커밋: `2c8ce87`

### [서비스 섹션] 22:33
- 변경 내용:
  1. index.html #features: "55개 이상 도시" / "글로벌 도시 현지 단가 데이터베이스" / "견적 가능
     글로벌 도시" 표현을 사이트 전체(히어로 trust-strip, 메타 디스크립션, 갤러리 소개문)가 쓰는
     "55개국(현지 파트너 보유 국가)"에 맞춰 "55개국" / "55개국 현지 파트너·단가 데이터베이스" /
     "견적 가능 파트너 국가"로 통일. (도시 수 vs 국가 수로 다르게 읽히던 미묘한 불일치 수정)
  2. styles.css .svc-item: 행 전체에는 hover 반응이 전혀 없어 정적으로 느껴지던 문제 → 배경의
     대형 장식 숫자(::before)에 transition 추가하고 :hover 시 rgba(200,16,46,.08)로 은은한 붉은
     틴트가 들어가도록 추가. 레이아웃/폰트/그리드는 변경 없음.
- GPT 피드백 요약: "55개국으로 통일하는 것이 B2B 담당자에게 일관되고 신뢰할 수 있는 메시지를
  준다는 점에서 타당함. hover는 전체 행에 은은한 피드백을 주는 방향이 UX 개선에 맞고, 제안한
  rgba(200,16,46,.07~.10) 범위가 과하지 않고 적절함." 추가로 모바일 가독성/터치영역 점검과
  지표열(180px) 강조 여부를 검토해보라는 제안 있었음 → 확인 결과 모바일 브레이크포인트에서는
  이미 grid-template-columns:1fr 로 전환되고 ::before는 display:none 처리되어 있어(스타일시트
  1086~1090행) 문제 없음을 확인, 지표열은 현재도 충분히 강조되어 있어(44px bold 숫자) 추가 변경
  불필요로 판단.
- 검증 결과: 통과. verify_render.py로 콘솔 에러 확인(베이스라인과 동일한 8건의 CDN/폰트 오류만,
  신규 에러 없음). 별도로 #features 섹션만 스크롤 후 스크린샷(정상 상태/hover 상태) 육안 확인 —
  텍스트 정상 반영, hover 시 배경 숫자에 붉은 틴트가 은은하게 들어가는 것 확인, 레이아웃 깨짐 없음.
- 커밋: `cb8fd60`

### [포트폴리오 섹션] 22:38
- 변경 내용:
  1. index.html #portfolio: 18번 카드(크로아티아 그린시티 연수)만 유일하게 지역명 앞에 국기 이모지
     (🇭🇷)를 붙이고 있어 나머지 17개 카드(플레인 텍스트 "국가 · 도시")와 불일치했음. Windows/Segoe
     UI 환경에서는 국기 이모지 글리프가 없어 실제로는 깨진 2글자 알파벳으로 보일 위험이 있어(B2B
     담당자가 회사 PC/윈도우에서 볼 가능성 높음) 이모지 제거하고 "크로아티아"로 통일.
  2. **사이트 전역 버그 발견 및 수정**: .site-header가 position:fixed, height:64px(모바일 56px)인데
     scroll-margin-top이 어디에도 설정되어 있지 않아, 상단 네비게이션에서 앵커 링크(#destinations
     #estimate #portfolio #testimonials #faq #about #contact)를 클릭하면 스크롤 정지 후 섹션 상단
     ~31px가 고정 헤더 뒤에 가려지는 문제를 Playwright로 실측 확인(수정 전 #portfolio top=31.3px,
     헤더 높이=64px). styles.css에 `#home,#destinations,#estimate,#portfolio,#testimonials,#faq,
     #about,#contact { scroll-margin-top: 84px; }` 추가(모바일 브레이크포인트에서는 72px로 축소,
     헤더 56px 기준). 이 문제는 포트폴리오 섹션 점검(nav 링크 "포트폴리오" 클릭) 중 발견했지만
     사이트 전체 앵커 이동에 영향을 주는 이슈라 함께 수정.
- GPT 피드백 요약: 이모지 제거에 동의(리스크 거의 없음, 일관성 개선). scroll-margin-top 방식이
  JS로 앵커 클릭을 가로채 offset 계산하는 방식보다 더 간단하고 안전하며 성능/유지보수 면에서
  유리하다고 확인. 추가로 alt 텍스트/반응형/더보기 로드 성능/네이버 블로그 링크 유지보수 리스크를
  짚어줬는데, alt 텍스트는 이미 각 카드에 존재하고 lazy loading도 이미 적용되어 있어 추가 조치
  불필요로 판단. 블로그 포스트 링크의 실제 생존 여부는 오프라인 환경이라 확인 불가 — 아래 참고.
- 검증 결과: 통과. Playwright로 실제 "포트폴리오" 네비 링크 클릭 재현 → 수정 후 #portfolio
  top=127.3px로 헤더(64px) 아래 충분히 노출됨 확인. 크로아티아 카드 텍스트("크로아티아\n그린시티
  국외 교육연수") 육안/텍스트 확인, 이모지 없음. "더보기" 버튼 클릭 후 카드 정상 노출, 모바일
  햄버거 메뉴도 정상 오픈 확인. verify_render.py 콘솔 에러는 베이스라인과 동일(8건, CDN/폰트).
- 다음에 사용자와 논의 필요: 포트폴리오 카드가 링크하는 네이버 블로그 포스트(예:
  blog.naver.com/hanaenbt/224293285292 등 18개)가 실제로 살아있는 게시물인지는 오프라인
  샌드박스에서 네트워크 접근이 안 되어 확인 불가 — 사용자가 직접 몇 개 샘플 클릭해 링크 생존
  여부 확인 권장.
- 커밋: `53b8588`

### [후기 섹션] 22:42
- 변경 내용: index.html #testimonials 카드 4개의 별점("★★★★★")이 순수 텍스트라 스크린리더가
  의미 없이 별 글자를 반복해서 읽던 문제 → `role="img" aria-label="5점 만점에 5점"`을 4곳 모두
  추가(replace_all). 시각적 변화 없음, 마크업만 보강.
- GPT 피드백 요약: aria-label 방식 적절하다고 확인. 후기 4개가 전부 "김○○/박○○" 식으로
  익명화된 것은 B2B 특성상(기업정보 보호) 자연스럽고 신뢰도에 큰 영향 없다는 의견 — 추가 조치
  안 함. trust-strip 시각적 임팩트는 색상대비/타이포/구분선 관련 일반적 제안이 있었으나 검토 결과
  이미 32px bold 숫자 + red 강조 유닛 + 구분선이 적용되어 있어 추가 변경은 보류(과잉 수정 방지).
- 검증 결과: 통과. verify_render.py 콘솔 에러 베이스라인과 동일(8건). 스크린샷으로 레이아웃 변화
  없음 확인(마크업 속성 추가만이라 시각적 회귀 위험 자체가 낮았음). trust-strip은 별도로 스크롤해
  스크린샷 확인 — 4개 지표 정상 표시(98% / 1,400+ / 55+ / 4.9/5), 문제 없음.
- 커밋: `3e8d5fb`

