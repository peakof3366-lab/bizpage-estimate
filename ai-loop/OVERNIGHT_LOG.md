# 오버나이트 작업 로그 (2026-07-04 밤)

## 추가: 2026-07-07 낮 — 견적/문의 서버 저장 + 관리자 인증 서버화 (최우선 구조적 이슈 해결)

이전 세션들에서 반복적으로 "최우선 미해결" 항목으로 기록되던 두 가지를 사용자와
합의한 계획(EnterPlanMode)에 따라 실제로 구현:

1. **견적/문의가 localStorage에만 저장되던 문제 해결** — Vercel Postgres(Neon 마켓플레이스
   통합, 무료 티어) 프로비저닝 후 `quotes`/`inquiries`/`admin_auth` 3개 테이블 생성.
   `/api/quotes`, `/api/quotes/[id]`, `/api/inquiries`, `/api/inquiries/[id]` 서버리스
   함수 신규 작성(공개 POST 제출 + 관리자 전용 GET/PATCH/DELETE). `script.js`의 3곳
   (`saveFullEstimate()`, 문의폼 제출, `submitConsult()`)에서 기존 localStorage 저장은
   그대로 유지한 채 서버 POST를 추가(fire-and-forget, 실패해도 로컬 저장은 살아있음).
2. **관리자 로그인 클라이언트 사이드 평문 비교 → 서버 인증으로 전환** — `admin_auth`
   테이블에 bcrypt 해시로 비밀번호 저장(하드코딩된 `hanaenbt` 폐기, 사용자가 새
   비밀번호 직접 지정). `jose` JWT + httpOnly 쿠키(`SESSION_SECRET` 환경변수 서명,
   12시간 만료)로 세션 발급. `/api/admin/login`·`logout`·`change-password`·`me` 구현.
   `admin.html`은 로그인 성공 후 `loadRemoteData()`로 서버 데이터를 기존 localStorage
   캐시 키(`linkedt_contacts`, `linkedt_estimates_full`)에 채워 넣는 방식으로 연결해
   기존 렌더링/필터/페이지네이션 코드는 거의 그대로 재사용, 상태/메모 저장·삭제·전체
   삭제 지점에만 대응 API 호출을 추가.

검증: `vercel dev` 로컬 서버로 모든 API 엔드포인트 curl 테스트(로그인 성공/실패,
인증 없이 401, PATCH/DELETE, change-password) 통과. Playwright로 **핵심 성공 기준**
검증(`ai-loop/test_server_sync.py` 신규) — 완전히 분리된 두 브라우저 컨텍스트(쿠키/
localStorage 전혀 공유 안 함)로 "방문자가 견적+문의 제출 → 전혀 다른 브라우저에서
관리자 로그인 → 방금 제출한 항목이 보임"을 실제로 확인, PASS. `verify_render.py`로
index.html 회귀 없음 확인. admin.html은 이제 서버 없이(file://) 열면 `/api/admin/me`
fetch가 의도적으로 실패하는데(로컬 백엔드 없이 인증 자체가 불가능해졌으므로 당연한
동작), 이는 회귀가 아니라 설계상 필연적 변화로 확인함 — admin.html은 이제 반드시
실제 배포(또는 `vercel dev`) 환경에서만 동작함.

### 아직 사용자가 판단할 것
- 실제 배포(master push)는 아직 하지 않음 — 사용자 확인 후 진행 예정.
- `ai-loop/itinerary_api.py`(로컬 전용 동적 일정 API)는 이번 작업 범위 밖, 여전히 미해결.
- `linkedt_estimates`(경량 분석 통계)는 이번에 의도적으로 로컬 전용 유지.

## 추가: 2026-07-07 새벽(2차) — "견적 부분 오류를 더 잡을 방법?" 요청에 대응

### A. 견적 계산 로직 대규모 조합 퍼징 (버그 0건)
`ai-loop/fuzz_estimate.py`/`fuzz_estimate2.py` 신규 작성. 55개 목적지 전체 x
인원수(경계값 포함) x 일수 x 프로그램/기관유형 x 좌석등급 x 호텔등급 x
객실구성(VIP 포함) x 출발공항 x 시즌 x 포함항목 개별 해제까지 총 167,418건
조합을 브라우저 컨텍스트에서 자동 생성해 getBreakdownData()를 직접 호출,
NaN/음수/단조성/1인당 금액 정합성/비즈니스≥이코노미 등 불변조건 전수
검사 — **위반 0건**. 계산 로직 자체는 견고함을 확인.

### B. 견적서 공유페이지·인쇄팝업 XSS 취약점 발견 및 수정 (중요)
계산 로직이 아닌 "출력" 쪽을 점검하다가 실제 보안 취약점을 발견:
- estimate-view.html은 URL의 `?d=` 파라미터(base64 JSON)를 서버 검증 없이
  그대로 innerHTML로 삽입하고 있었음. 이 파라미터는 누구나 URL만 조작하면
  임의 문자열을 넣을 수 있어, 목적지명·기관명·요청사항·일정 텍스트 등
  어디에든 `<img src=x onerror=...>` 같은 페이로드를 넣으면 그 링크를 여는
  사람(담당 직원 등)의 브라우저에서 그대로 실행되는 XSS였음.
- script.js의 견적서 인쇄 팝업(`document.write`)에도 기관명/담당자/
  요청사항이 동일하게 이스케이프 없이 삽입되고 있었음.
- admin.html은 이미 자체 `esc()` 헬퍼로 방어되어 있어 문제 없음(확인만).

수정: 두 파일에 HTML 이스케이프 헬퍼(`esc`/`_escHtml`) 추가 후 모든 동적
삽입 지점에 적용. 실제 페이로드(`<img onerror>`, `"><script>` 등)를 두
경로(estimate-view.html 직접 로드 / index.html 실입력→인쇄팝업) 모두에
주입해 실행되지 않음을 Playwright로 검증(`ai-loop/test_xss_fix.py`, PASS).

### C. 목적지별 데이터 9종 테이블 재대조, 포트폴리오 블로그 링크 18개 생존
확인, 호주 요율 존 오분류(실제 항공권 시세 대조 후) 수정 — 각각 상세는
아래 항목 참고.

검증: node --check 통과, verify_render.py + interaction_probe(estimate)
콘솔 에러 0건, Vercel 프로덕션 배포와 로컬 파일 md5 동일 확인(script.js,
estimate-view.html 둘 다).
커밋: `51e6ab4`(퍼징 스크립트) → `475c1f7`(XSS 수정)

---

## 추가: 2026-07-07 새벽 — 사용자 복귀 후 후속 요청("노출 안 된 부분 더 체크, 웹에서 정보 찾아 연결")

1. **목적지별 데이터 테이블 9종 전체 재스캔** — BIZ_ZONES/DEST_MAP_FRAC/
   DEST_IMAGES/DEST_PARTICIPANT_NOTE/DEST_PHOTOS/ITINERARY_DB/
   PROGRAM_PRIORITY/destinationRates/DEST_REC 전부 55개 옵션 목록과 재대조.
   destinationRates는 필드명(`destination_key`)을 이전에 잘못 매핑해 검증이
   부정확했던 것을 바로잡아 재확인 — 완전히 1:1 일치, 문제 없음.
2. **포트폴리오 네이버 블로그 링크 18개 생존 확인** — 이전 세션들에서
   "오프라인 샌드박스라 확인 불가"로 계속 미해결 남아있던 항목. 이번엔
   Playwright로 blog.naver.com에 실제 접근 가능함을 확인하고(이 샌드박스는
   예외적으로 네트워크가 열려있음), PostView.naver iframe 본문까지 읽어
   18개 전부 실제 살아있는 포스트임을 확인함(제목 예: "그린시티 국외
   교육연수 [크로아티아]", "몽골 환경 부처 공무원 초청 연수 [서울]" 등).
   커밋 대상 코드 변경 없음(원래도 정상 링크였음).
3. **호주(브리즈번·골드코스트) 요율 존 오분류 수정** — 어젯밤 "항공료는
   150만원인데 zone은 시드니/멜버른과 같은 mid(3.2배수)로 묶여있어 다른
   150만원대 목적지(전부 long/4.0배수)와 가격 일관성이 깨진다"고 보고만
   해뒀던 건을, 웹 검색으로 실제 항공권 시세를 대조해 해결:
   - 인천-시드니 콴타스 왕복 이코노미 ≈ 1,101,200원 (사이트 시드니 요율
     1,100,000원과 거의 일치 — 사이트 요율이 실제 시세 기반임을 재확인)
   - 인천-브리즈번은 대한항공 논스톱 기준 약 $1,186(≈160만원)로 시드니보다
     확연히 비쌈 — 취항 항공사가 대한항공 단독이라 경쟁이 적은 것이 원인으로
     추정
   → 항공료 숫자(150만원) 자체는 실제 시세에 부합하는 정확한 값이었고,
   **zone 분류가 잘못된 것**으로 결론. `BIZ_ZONES.mid`에서 `long`으로 이동.
   런타임에서 `getBizFactor('호주')`가 4(long)를 정상 반환함을 확인.
   커밋: `5f22ddf`

검증: node --check 통과, verify_render.py + interaction_probe(estimate) 콘솔
에러 0건, Vercel 프로덕션 배포와 로컬 script.js md5 동일 확인.

---

## 최종 요약 (2026-07-07 새벽 — 사용자 취침 중 자율 진행분)

사용자가 "오늘도 승인 처리 직접 해주고, 오류 체크 계속하고, 아침에 올 때까지
진행해달라"고 요청 후 취침 → 아래 항목들을 승인 대기 없이 순서대로 진행:

1. **목적지 사진 공백 25개 해소** — DEST_IMAGES/DEST_PHOTOS 어느 쪽에도 없어
   STEP3·견적서에 사진이 전혀 안 뜨던 25개 목적지에 Unsplash 사진(cover+strip
   7~8장, 총 190여 장) 신규 추가. 각 장을 curl로 HTTP 200 + image content-type
   개별 확인 후 채택, Playwright로 실제 렌더링(하와이 샘플)까지 스크린샷 확인함
   — 이 샌드박스는 예외적으로 images.unsplash.com 접근이 가능해 실사 로딩까지
   육안 확인됨. 이제 55개 목적지 전원이 사진을 보유. (커밋 `9bf4dc3`)
2. **destinationRates 수치 전수 스캔** — 0/음수/누락 0건, vehicle_small >
   vehicle_large 역전 0건. 값 범위 자체는 전부 합리적(항공료 26~190만원,
   장거리일수록 비쌈 등). 단, **호주(브리즈번/골드코스트) 항공료가 150만원으로
   같은 요금대의 다른 목적지(로스앤젤레스/하와이/밴쿠버/토론토/영국 등)는 전부
   "long"존(비즈니스석 4.0배수)인데 호주만 "mid"존(3.2배수)으로 분류되어 있어
   가격 일관성이 깨짐** — 시드니/멜버른(110만원)·오클랜드(120만원)와 묶여서
   지역별로 분류된 탓으로 추정. 실제 항공료 자체가 잘못됐는지, 존 분류가
   잘못됐는지는 요율 데이터 소스가 없어 판단 불가 — **가격(매출) 데이터라
   임의로 고치지 않고 여기 기록만 남김. 사용자 판단 필요.**
3. **ITINERARY_DB 지리/사실관계 2차 정밀 검토** — 1차(어젯밤)에 이어 추가로
   시간대 논리 오류 없는지, 기관/장소가 실제 위치와 맞는지 재확인:
   - 네덜란드 코스 day3: ASML(에인트호벤 소재, 암스테르담에서 130km)을 이동
     언급 없이 방문하는 것처럼 보이던 부분에 "에인트호벤 이동"/"델프트 이동"
     명시 추가 (커밋 `ddd629f`)
   - 이외 "당일치기" 표기가 있는 구간(나고야/유후인/오타루/심천/나보이)은 전부
     실제로 가능한 거리에 캐비엇까지 붙어있어 문제 없음을 확인
4. **PROGRAM_PRIORITY 41개 목적지 미매핑** — 신규 목적지들은 코스가 2종(벤치
   마킹형/팀빌딩형)뿐이라 매핑이 없어도 기본 순서([0,1])가 이미 산업/학술
   유형에는 맞고, 언어/리더십 유형에 어떤 순서가 더 나을지는 정답이 없는 취향
   판단이라 임의로 코스 노출 순서를 바꾸지 않기로 결정(로그만 남김).
5. **admin.html / estimate-view.html 재검증** — 오늘 변경사항과 무관하지만
   회귀 여부 확인 차 재실행, 콘솔 에러 0건.

### 오늘 밤 커밋 목록
`9bf4dc3`(사진 25개 추가) → `ddd629f`(ASML 이동 명시 + 사진 검증 테스트)
→ `7dd3121`(estimate-view 사진 렌더링 테스트) — 전부 push 완료, Vercel
프로덕션과 로컬 script.js md5 동일 확인함(배포 정상).

### 아침에 사용자가 판단할 것
- **호주 존 분류 vs 항공료 불일치**(위 2번) — 어느 쪽이 맞는 데이터인지
  실제 항공권 시세 확인 후 알려주시면 그에 맞춰 한쪽을 수정하겠음.
- 그 외 이전에 보고했던 "네이버 블로그 포스트 생존 여부"(포트폴리오 섹션)도
  여전히 사용자 직접 확인 필요 상태로 남아있음.

---

## 추가: 2026-07-06 밤(2차) — 전체 55개 목적지 심층 재검토(사용자 요청 "전부 재확인, 심오하게 깊게")

사용자가 이전 배포 지연 이슈(push 안 됨)를 지적한 뒤, "전부 다시 재확인하고
부족한 부분을 채워달라"고 재요청. 이번엔 신규 31개뿐 아니라 원본 24개
(도쿄~대만)까지 포함해 전체 55개 목적지 × 2~3코스 × 5일(약 550개 day 항목)을
전수 대상으로 삼아 다음을 수행:

1. **배포 상태 재검증**: `vercel ls`로 실제 프로덕션 배포가 push 3분 만에
   Ready 상태임을 확인, `curl`로 라이브 script.js와 로컬 파일 md5sum 동일함을
   재확인(WebFetch 자체 캐시로 처음엔 구버전처럼 보였으나 curl 직접 비교로 최종
   확인). 배포 정상.
2. **데이터 완비성 크로스체크**: index.html의 목적지 select(55개 옵션) 기준으로
   destinationRates·BIZ_ZONES·DEST_REC·DEST_MAP_FRAC·ITINERARY_DB·
   DEST_IMAGES·DEST_PHOTOS·DEST_PARTICIPANT_NOTE·PROGRAM_PRIORITY 9개 데이터
   테이블을 전부 추출해 옵션 목록과 1:1 대조하는 스크립트 작성 후 실행.
3. **시간대 논리 자동 스캔**: 전체 ITINERARY_DB의 am/pm/eve 필드에서
   "오전에 석양/야경/야시장이 나오는" 물리적으로 불가능한 패턴을 정규식으로
   전수 스캔 → 5건 발견(치앙마이/카자흐스탄/호주/동유럽/토론토), 전부 수정.
4. **원본 24개 목적지 재독해**: 신규 31개는 지난 라운드에 이미 정독했으나
   원본 24개는 이번이 최초 전수 재독해. 지리적 타당성 오류 2건 발견·수정
   (호주 골드코스트 코스의 "그레이트 배리어 리프 당일치기"는 실제로는
   케언즈 인근까지 1,700km+ 떨어져 있어 물리적으로 불가능 → 실제 근교인
   모튼 아일랜드로 교체 / 몽골 코스의 "아르항가이 화산 지형"은 울란바토르에서
   약 400km 떨어져 있어 같은 날 징기스칸 기마상 방문 후 귀환과 양립 불가능
   → 테를지 국립공원 내 실제 명소로 교체).
5. **완성도 보완**: DEST_PARTICIPANT_NOTE에서 직접 항목도 `_FALLBACK_MAP`
   경유 항목도 전혀 없던 유일한 목적지(오키나와)에 신규 노트 추가 — 55개
   전원 커버 완료.

### 이번에 손대지 않고 "발견만" 해서 보고한 항목 (사용자 판단 필요)
- **목적지 사진 공백**: DEST_IMAGES(로컬)·DEST_PHOTOS(Unsplash) 어느 쪽에도
  등록되지 않아 STEP3/견적서에 사진이 전혀 안 뜨는 목적지가 25개
  (오키나와/장가계/청도/연태/가오슝/다낭/나트랑/푸꾸옥/보홀/코타키나발루/
  푸켓/치앙마이/라오스/카자흐스탄/호주/사이판/네덜란드/동유럽/북유럽/서유럽/
  샌프란시스코/워싱턴/하와이/밴쿠버/토론토). 코드 자체는 사진 없을 때 안전하게
  숨김 처리되어 레이아웃 깨짐은 없음(defensive `onerror`/조건부 렌더 확인함).
  다만 실제 사진 소싱(Unsplash 등)은 외부 네트워크로 URL을 검증해야 하는
  별도 작업 규모라 이번엔 진행하지 않고 사용자에게 먼저 보고.
- **PROGRAM_PRIORITY 커버리지**: 55개 중 14개(원본 목적지 위주)만 프로그램
  유형별(언어/리더십/산업/학술) 코스 우선순위 매핑 보유. 나머지 41개(신규
  배치 전부)는 매핑이 없어 프로그램 유형과 무관하게 항상 courses[0]/[1]
  순서 그대로 노출됨 — 신규 목적지는 애초에 코스가 2개(벤치마킹형/팀빌딩형)뿐이라
  체감 영향은 제한적이나, 완전한 기능 패리티를 원하면 확장 여지 있음.

검증: `node --check` 통과, ITINERARY_DB 구조 전수 재검증(필드 누락 0건),
`verify_render.py` 콘솔 에러 0건(`ai-loop/logs/verify_20260706_233741_deep_reaudit_fix/`).
커밋: `569a371`

## 추가: 2026-07-06 밤 — 신규 31개 목적지 콘텐츠 품질 재검토(사용자 요청)

3차 라운드에서 추가한 31개 목적지 × 2코스 × 5일 일정(약 310개 day 항목)을
전수 정독하며 논리/사실 오류를 찾는 방식으로 재검토. 발견한 3건 수정:

1. **코타키나발루 벤치마킹 코스(산업형) day5**: 귀국일 오전(am) 슬롯에 "세계
   3대 석양 감상·자유시간"이 들어있었음 — 오전에 일몰을 볼 수 없는 논리 오류.
   이 코스의 highlights에는 애초에 석양이 없었고(석양은 코스2 "석양·자연
   팀빌딩 코스"의 테마), 다른 30개 목적지의 귀국일 오전은 전부 "자유시간·쇼핑"
   패턴이라 동일하게 정정.
2. **스페인(바르셀로나) 벤치마킹 코스**: highlights와 day3에 "IE 비즈니스스쿨"이
   등장하는데 IE 비즈니스스쿨은 실제로는 마드리드 소재 — 바르셀로나 일정에
   배치된 사실 오류. 바르셀로나 소재 명문 경영대학인 ESADE 비즈니스스쿨로 수정.
3. **북유럽 벤치마킹 코스**: highlights에 "알토대학"(핀란드 헬싱키 소재)이
   있었는데 실제 5일 일정에는 스톡홀름(스웨덴)·코펜하겐(덴마크)만 등장하고
   헬싱키 방문이 전혀 없어 highlights에서 제거. 또한 day4에서 "덴마크 복지
   행정 현장 방문"이 이동 언급 없이 갑자기 등장(day1~3은 스톡홀름 기준)하던
   부분을 "코펜하겐 이동"을 명시해 동유럽/서유럽 등 다른 다국가 코스와
   표기 방식 일관성 확보.

전수 정독 중 위 3건 외에는 지명·기관명·시간대 배치가 대체로 일관되고
타당한 것으로 확인(예: 앙코르와트/보홀 초콜릿힐/할레아칼라 등 일출은 전부
am에, 대부분의 야경/석양 이벤트는 eve에 올바르게 배치됨).
검증: `node --check script.js` 통과, `verify_render.py` 콘솔 에러 0건
(`ai-loop/logs/verify_20260706_231618_content_quality_fix/`).
커밋: `7a70726`

## 추가: 2026-07-06 낮 (3차) — ITINERARY_DB 미등록 31개 목적지 콘텐츠 전량 추가

이전 라운드에서 제안한 "다음 실질 작업" 후보(ITINERARY_DB에 직접 등록되지 않아
API/제네릭 폴백으로 코스가 생성되던 31개 목적지)를 5개 배치로 나눠 전량 완료:
- Batch A(6): 오키나와/장가계/청도/연태/마카오/가오슝 (`d3e74d3`)
- Batch B(8): 나트랑/푸꾸옥/마닐라/보홀/코타키나발루/캄보디아/치앙마이/라오스 (`2af826d`)
- Batch C(5): 카자흐스탄/멜버른/호주/오클랜드/사이판 (`ba01dc3`)
- Batch D(6): 영국/네덜란드/스페인/동유럽/북유럽/서유럽 (`0381bec`)
- Batch E(6): 로스앤젤레스/샌프란시스코/워싱턴/하와이/밴쿠버/토론토 (`6fc8224`)

각 목적지마다 기존 DEST_REC(a=역량강화형/b=동기부여·화합형 태그, points/items,
value)를 스캐폴딩으로 활용해 5일 상세 일정(am/pm/eve/tip) × 2코스를 작성.
배치별로 `_hasDirectEntry()`/`getItineraries()` 스모크 테스트 + `verify_render.py`
콘솔 에러 확인 후 커밋. 마지막에 전체 55개 목적지 재검증 — `_hasDirectEntry()`
전부 true, 콘솔 에러 0건 확인. 이로써 견적 시스템의 모든 목적지가 API 의존 없이
자체 리치 일정을 보유하게 됨.

## 추가: 2026-07-06 낮 (2차) — 데이터 정합성 전수 검증, 코드 변경 없음

큐 점검 이후, 과거에 실제 버그를 찾아냈던 방식(코드 참조 vs 실제 정의 불일치)으로
전수 스윕을 돌림:
1. `<select id="destination">` 55개 옵션 전체를 순회하며 각 목적지로 견적 계산 +
   비즈니스석(getBizFactor 폴백 경고 트리거 조건) 강제 실행 → 콘솔 경고/에러/NaN
   발생 0건.
2. `destinationRates` 55건 전수: option 목록과 1:1 일치, 중복 키 없음, 필수 필드
   (항공/유류/호텔/식사/차량/가이드/관광/마진/rateDate) 누락·0이하 값 없음.
3. index.html의 `onclick="함수(...)"` 8종 전부 script.js에 실제 정의되어 있음 확인.
4. script.js의 `getElementById(...)` 참조 84건 전수 대조 — 매칭 안 되는 3건
   (`_apiToast`, `_spinStyle`, `photoLightbox`)은 모두 `createElement` 후 `.id`를
   런타임에 부여하는 지연 생성 싱글턴 패턴으로, 죽은 참조 아님을 확인.
5. RATE_META 기준 전체 요율이 2026-06 하나로 일관, 다음 갱신 예정(2026-09) 전이라
   데이터 신선도 문제 없음.

결론: 이번 라운드도 실제 수정 대상 버그는 발견되지 않음. 남은 실질적 작업은 버그
탐색이 아니라 **콘텐츠 확장**(ITINERARY_DB에 직접 등록되지 않은 31개 목적지 —
오키나와/장가계/청도/연태/마카오/가오슝/나트랑/푸꾸옥/마닐라/보홀/코타키나발루/
캄보디아/치앙마이/라오스/카자흐스탄/멜버른/호주/오클랜드/사이판/영국/네덜란드/
스페인/동유럽/북유럽/서유럽/로스앤젤레스/샌프란시스코/워싱턴/하와이/밴쿠버/토론토
— 는 API/제네릭 폴백으로 코스가 생성되어 다른 목적지 대비 일정 품질이 낮음)로
판단됨.

## 추가: 2026-07-06 낮 — 남은 큐(5~9번) 점검, 코드 변경 없음

OVERNIGHT_PLAN.md 큐의 나머지 항목(회사소개/문의 나머지/STEP3 UI/estimate-view.html/
크로스커팅)을 순서대로 점검. 실제 버그로 이어진 항목은 없었고, 아래 3건은 조사 중
"버그처럼 보였다가 재현 방법이 잘못된 것으로 판명"된 케이스라 기록해 둠(다음에 같은
방식으로 재조사할 때 시간 낭비 방지):

1. **STEP3 세계지도가 빈 흰 박스로 보이는 문제** — `shot_section.py`로 스크린샷했을 때
   지도가 깨진 이미지 아이콘만 보이는 것처럼 나왔음. 원인 조사 결과, 테스트 스크립트가
   `renderStep3()` 호출 후 `#step3Section`의 `hidden` 클래스를 **직접** 제거해서 발생한
   아티팩트였음. 실제 사용자 흐름(`explorePlanBtn` 클릭 → `scrollToStep3()`)은 섹션을
   먼저 unhide한 뒤 `_positionDestMapPin()`을 재호출하는 기존 수정(주석 참고:
   script.js:3297 "섹션이 숨겨져 있던 동안엔 지도 프레임 크기를 읽을 수 없었으므로")이
   이미 들어가 있어 정상 동작 확인(`img.style.width` 등이 유효한 px 값으로 재계산됨,
   스크린샷으로 한국·목적지 핀 정상 표시 확인).
2. **STEP3 코스A/B "탐색하신 일정" 배지가 안 보이는 것처럼 보인 문제** — 전체 페이지
   스크린샷을 축소해서 봐서 안 보였을 뿐, `.iti-card-selected` 클래스와 배지 모두 정상
   적용됨을 요소 단위 스크린샷으로 재확인.
3. estimate-view.html 데스크톱/모바일/인쇄(`emulate_media('print')`) 3종 모두 실제
   shareData로 렌더링해 확인 — 유효기간 배너/CTA/nav가 인쇄 시 정상 제외, 모바일
   반응형 정상, 오류 없음.

회사소개(파트너 chips, 사진 레이아웃)와 문의 섹션(구글맵 임베드, 문의 폼 성공메시지)도
코드/스크린샷으로 재확인했으나 이미 이전 라운드에서 정리된 상태라 추가 수정 불필요.
네비게이션 햄버거 메뉴 aria-expanded 토글도 정상 동작 확인(script.js:678-682).
`verify_render.py`로 최종 베이스라인 재확인 — 신규 콘솔 에러 없음.

결론: 코드 변경 없음(안전 규칙 4번 — 확실한 버그가 아니면 무리하게 손대지 않음).
현재 사이트는 지난 라운드들에서 이미 충분히 다듬어진 상태로 판단됨.

## 추가: 2026-07-06 새벽 — 백그라운드 에이전트 승인 프롬프트 문제로 방식 전환

백그라운드 에이전트(worktree 격리 포함) 방식이 반복적으로 사용자 승인 프롬프트를 유발해
(원인 특정 실패, 설정으로 해결 안 됨) 자정~새벽 사이 여러 차례 중단/재시도함. 최종적으로
에이전트 두 개(오류 헌팅, 견적정확도+일정보강)가 만든 worktree 브랜치를 모두 master로
병합하고, 이후 작업은 백그라운드 에이전트/worktree 없이 이 대화 세션에서 직접 진행하는
방식으로 전환. 병합 후 verify_render.py로 신규 콘솔 에러 없음 확인.

### [문의 섹션 검증 UX + 회사소개/푸터 정리] 01:03
- 변경 내용:
  1. script.js `inqForm` 제출 핸들러: 이름/연락처/문의내용 중 공백만 입력된 필드가 있을 때
     `if(!name||!tel||!msg) return;`로 아무 피드백 없이 조용히 무시되던 문제 발견(HTML5
     `required`는 공백만 있는 값을 통과시키므로 이 케이스에서 사용자는 버튼을 눌러도 아무
     반응이 없어 혼란스러웠을 것). 기존 상담폼(script.js:1047)과 동일한 `alert()` 패턴으로
     안내 메시지 추가.
  2. index.html #about: 실제 이미지(발리 해변 연수 사진)와 내용이 다른 잔존 HTML 주석
     ("한국 전문가 팀 이미지") 제거.
  3. 푸터 저작권 연도 2024 -> 2026 갱신.
- 검증 결과: 통과. verify_render.py 콘솔 에러 없음(베이스라인과 동일, 신규 없음).
- 커밋: `f40e6c3`

### [STEP3 방식 선택 카드 키보드 접근성] 06:42
- 변경 내용: `#planCardA`/`#planCardB`(방식 A/B 선택 카드)가 `onclick`만 있고 `tabindex`/
  `role`이 없어 키보드 사용자가 아예 포커스조차 할 수 없어 선택이 불가능했음.
  `role="button" tabindex="0"` 추가 + `onkeydown`으로 Enter/Space 시 `selectPlan()` 호출.
  `selectPlan()` 함수에도 `aria-pressed` 상태 토글 추가.
- 검증 결과: 통과. verify_render.py 콘솔 에러 없음. Playwright로 카드 포커스 후 Enter 키
  입력 시 실제로 선택(active 클래스 + aria-pressed="true")되는 것 확인.
- index.html 전체에서 `onclick` 있는 다른 `<div>` 중 `role="button"` 없는 것이 더 있는지
  grep으로 재확인 — 추가 발견 없음(이 두 카드가 유일한 사례였음).
- 커밋: `cbd503e`

## 추가: 2026-07-06 밤 (새 라운드) — 눈에 안 보이는 오류 헌팅 + 코드 교차검증

### [admin.html 레거시 텍스트] 00:05
- 변경 내용: 로그인 화면의 `.login-logo`가 리브랜딩 이전 브랜드명 "LINKED"를 그대로 표시하고
  있던 문제(어제 로그에 미수정으로 기록됨) 수정. 사이트 전체에서 쓰는 "비즈페이지"로 교체.
  순수 텍스트 치환, 스타일/구조 변경 없음.
- 검증 결과: 통과. verify_render.py로 admin.html 콘솔 에러 확인, 신규 에러 없음.
- 커밋: `c5acca3`

## 추가: 2026-07-05 아침 대화형 세션 요약

사용자 복귀 후 오버나이트 에이전트가 멈춘 지점(FAQ 마무리)을 이어받아 완료하고,
이후 사용자 요청으로 "연수 일정 탐색하기" 관련 작업을 이어감:

1. 견적 결과 카드 개선: 빈 상태(선택 전) 안내 강화 + 체크리스트 기본 접힘 (`f5e026a`)
2. STEP3 탐색기 핵심 버그 수정: 견적(itiA/itiB, ITINERARY_DB)과 탐색기(기존 DEST_REC)가
   서로 다른 데이터를 써서 프로그램유형에 따라 내용이 달라지던 문제 -- getItineraries()로
   소스 통일, 공유링크에 programType(pt) 파라미터 추가 (`f1d3dad`)
3. "연수 일정 탐색하기" 버튼: 빨간 강조 스타일로 변경 + 버튼 순서 맨 앞으로, 스크롤 대상을
   `.estimate-grid`로 변경해 "견적서 받기"와 동시에 보이도록 수정. 부수적으로
   "이 견적으로 바로 상담 신청하기" 버튼이 실제로는 한번도 노출된 적 없던 죽은 버튼이었던
   것도 발견해 수정 (같은 커밋)
4. STEP3 사진: DEST_IMAGES(로컬) 폴백 연결로 14 -> 19개 목적지 (`e576571`), 이어서 실제
   Unsplash 검색 결과에서 검증된 URL로 11개 목적지 신규 추가 -> 총 30개 목적지 사진 보유
   (`b901af0`)
5. STEP3에 목적지 위치 세계지도 추가 (Wikimedia 퍼블릭도메인 SVG 자체 호스팅,
   전체 55개 목적지 좌표 매핑, 적도/본초자오선 기준점으로 좌표 변환 검증) (`5a4f38e`)

모든 변경 Playwright로 검증 완료, git 커밋 완료. 다음 후보: 회사소개/문의 섹션,
포트폴리오 블로그 링크 생존 확인(사용자 직접), 파트너 로고 Clearbit 의존성 개선,
지도(Google Maps) 임베드 실패 폴백.

## 추가: 2026-07-05 오전 후속 요청 처리

사용자가 세 가지를 추가 요청:
1. "연수 일정 탐색하기"를 "견적서 받기"와 같은 위치로 이동
2. STEP3 선택 내용이 견적서에 반영 안 되는 문제 재점검
3. 세계지도를 대한민국 기준으로 확대되게 개선

처리 결과:
1. 버튼을 estimateConfirm 패널 안, 견적서 받기 바로 위로 실제 이동. 견적서 받기는
   보조 스타일(검정)로 변경해 우선순위 명확화 (`31305f2`)
2. **중대 버그 발견**: STEP3의 견적서 다운로드 버튼이 `openEstimateWindow()`(현재
   정상 함수)가 아니라 `downloadEstimateWithPlan()` -> `downloadEstimateFile()`라는
   완전히 별도의 리브랜딩 이전 레거시 함수(브랜드명 "LINKED" 잔존)를 호출하고 있어서,
   STEP3에서 고른 플랜이 실제 공유용 견적서에는 전혀 반영되지 않았음. 레거시 함수
   삭제하고 openEstimateWindow()로 통일 + shareData에 선택 플랜(sp) 추가해
   estimate-view.html과 즉시 인쇄 창 모두에 "탐색하신 일정" 배지 표시되도록 수정 (`b42df08`)
3. 위경도 기반 bbox 자동 확대(cover 크롭) 구현. 처음엔 min() 사용으로 여백 버그,
   이후 지도 동쪽 경계(180도) 근처에서 확장 범위가 넘어가는 문제 있었으나 경계
   보정 로직 추가로 해결, 실측값으로 검증 (`a217ab5`)

참고로 admin.html 로그인 화면에도 "LINKED" 레거시 텍스트가 남아있음을 발견 (미수정,
범위 밖).

### [사전 준비] 22:29
- OVERNIGHT_PLAN.md 확인, git 상태 확인(clean, baseline 커밋 존재), 베이스라인 verify_render 실행해
  "정상 상태에서도 뜨는 8건의 net::ERR_NAME_NOT_RESOLVED"가 CDN/폰트 오프라인 오류임을 재확인.
- ai-loop/logs/ 가 git 추적되고 있어 검증할 때마다 커밋이 무거워지는 문제 발견 → .gitignore에
  `ai-loop/logs/`, `ai-loop/tmp_prompt.txt` 추가하고 기존 추적 로그 git rm --cached 처리(파일은
  디스크에 그대로 있음, 삭제 아님). 커밋: `2c8ce87`

### [서비스 섹션] 22:33
- 변경 내용:
  1. index.html #features: "55개 이상 도시" / "글로벌 도시 현지 단가 데이터베이스" / "견적 가능
     글로벌 도시" 표현을 사이트 전체(히어로 trust-strip, 메타 디스크립션, 갤러리 소개문)가 쓰는
     "55개국(현지 파트너 보유 국가)"에 맞춰 "55개국" / "55개국 현지 파트너·단가 데이터베이스" /
     "견적 가능 파트너 국가"로 통일. (도시 수 vs 국가 수로 다르게 읽히던 미묘한 불일치 수정)
  2. styles.css .svc-item: 행 전체에는 hover 반응이 전혀 없어 정적으로 느껴지던 문제 → 배경의
     대형 장식 숫자(::before)에 transition 추가하고 :hover 시 rgba(200,16,46,.08)로 은은한 붉은
     틴트가 들어가도록 추가. 레이아웃/폰트/그리드는 변경 없음.
- GPT 피드백 요약: "55개국으로 통일하는 것이 B2B 담당자에게 일관되고 신뢰할 수 있는 메시지를
  준다는 점에서 타당함. hover는 전체 행에 은은한 피드백을 주는 방향이 UX 개선에 맞고, 제안한
  rgba(200,16,46,.07~.10) 범위가 과하지 않고 적절함." 추가로 모바일 가독성/터치영역 점검과
  지표열(180px) 강조 여부를 검토해보라는 제안 있었음 → 확인 결과 모바일 브레이크포인트에서는
  이미 grid-template-columns:1fr 로 전환되고 ::before는 display:none 처리되어 있어(스타일시트
  1086~1090행) 문제 없음을 확인, 지표열은 현재도 충분히 강조되어 있어(44px bold 숫자) 추가 변경
  불필요로 판단.
- 검증 결과: 통과. verify_render.py로 콘솔 에러 확인(베이스라인과 동일한 8건의 CDN/폰트 오류만,
  신규 에러 없음). 별도로 #features 섹션만 스크롤 후 스크린샷(정상 상태/hover 상태) 육안 확인 —
  텍스트 정상 반영, hover 시 배경 숫자에 붉은 틴트가 은은하게 들어가는 것 확인, 레이아웃 깨짐 없음.
- 커밋: `cb8fd60`

### [포트폴리오 섹션] 22:38
- 변경 내용:
  1. index.html #portfolio: 18번 카드(크로아티아 그린시티 연수)만 유일하게 지역명 앞에 국기 이모지
     (🇭🇷)를 붙이고 있어 나머지 17개 카드(플레인 텍스트 "국가 · 도시")와 불일치했음. Windows/Segoe
     UI 환경에서는 국기 이모지 글리프가 없어 실제로는 깨진 2글자 알파벳으로 보일 위험이 있어(B2B
     담당자가 회사 PC/윈도우에서 볼 가능성 높음) 이모지 제거하고 "크로아티아"로 통일.
  2. **사이트 전역 버그 발견 및 수정**: .site-header가 position:fixed, height:64px(모바일 56px)인데
     scroll-margin-top이 어디에도 설정되어 있지 않아, 상단 네비게이션에서 앵커 링크(#destinations
     #estimate #portfolio #testimonials #faq #about #contact)를 클릭하면 스크롤 정지 후 섹션 상단
     ~31px가 고정 헤더 뒤에 가려지는 문제를 Playwright로 실측 확인(수정 전 #portfolio top=31.3px,
     헤더 높이=64px). styles.css에 `#home,#destinations,#estimate,#portfolio,#testimonials,#faq,
     #about,#contact { scroll-margin-top: 84px; }` 추가(모바일 브레이크포인트에서는 72px로 축소,
     헤더 56px 기준). 이 문제는 포트폴리오 섹션 점검(nav 링크 "포트폴리오" 클릭) 중 발견했지만
     사이트 전체 앵커 이동에 영향을 주는 이슈라 함께 수정.
- GPT 피드백 요약: 이모지 제거에 동의(리스크 거의 없음, 일관성 개선). scroll-margin-top 방식이
  JS로 앵커 클릭을 가로채 offset 계산하는 방식보다 더 간단하고 안전하며 성능/유지보수 면에서
  유리하다고 확인. 추가로 alt 텍스트/반응형/더보기 로드 성능/네이버 블로그 링크 유지보수 리스크를
  짚어줬는데, alt 텍스트는 이미 각 카드에 존재하고 lazy loading도 이미 적용되어 있어 추가 조치
  불필요로 판단. 블로그 포스트 링크의 실제 생존 여부는 오프라인 환경이라 확인 불가 — 아래 참고.
- 검증 결과: 통과. Playwright로 실제 "포트폴리오" 네비 링크 클릭 재현 → 수정 후 #portfolio
  top=127.3px로 헤더(64px) 아래 충분히 노출됨 확인. 크로아티아 카드 텍스트("크로아티아\n그린시티
  국외 교육연수") 육안/텍스트 확인, 이모지 없음. "더보기" 버튼 클릭 후 카드 정상 노출, 모바일
  햄버거 메뉴도 정상 오픈 확인. verify_render.py 콘솔 에러는 베이스라인과 동일(8건, CDN/폰트).
- 다음에 사용자와 논의 필요: 포트폴리오 카드가 링크하는 네이버 블로그 포스트(예:
  blog.naver.com/hanaenbt/224293285292 등 18개)가 실제로 살아있는 게시물인지는 오프라인
  샌드박스에서 네트워크 접근이 안 되어 확인 불가 — 사용자가 직접 몇 개 샘플 클릭해 링크 생존
  여부 확인 권장.
- 커밋: `53b8588`

### [후기 섹션] 22:42
- 변경 내용: index.html #testimonials 카드 4개의 별점("★★★★★")이 순수 텍스트라 스크린리더가
  의미 없이 별 글자를 반복해서 읽던 문제 → `role="img" aria-label="5점 만점에 5점"`을 4곳 모두
  추가(replace_all). 시각적 변화 없음, 마크업만 보강.
- GPT 피드백 요약: aria-label 방식 적절하다고 확인. 후기 4개가 전부 "김○○/박○○" 식으로
  익명화된 것은 B2B 특성상(기업정보 보호) 자연스럽고 신뢰도에 큰 영향 없다는 의견 — 추가 조치
  안 함. trust-strip 시각적 임팩트는 색상대비/타이포/구분선 관련 일반적 제안이 있었으나 검토 결과
  이미 32px bold 숫자 + red 강조 유닛 + 구분선이 적용되어 있어 추가 변경은 보류(과잉 수정 방지).
- 검증 결과: 통과. verify_render.py 콘솔 에러 베이스라인과 동일(8건). 스크린샷으로 레이아웃 변화
  없음 확인(마크업 속성 추가만이라 시각적 회귀 위험 자체가 낮았음). trust-strip은 별도로 스크롤해
  스크린샷 확인 — 4개 지표 정상 표시(98% / 1,400+ / 55+ / 4.9/5), 문제 없음.
- 커밋: `3e8d5fb`

### [FAQ 섹션] 07:26 (백그라운드 에이전트 중단 후 이어받아 마무리)
- 변경 내용: FAQ 아코디언 버튼 7개에 `aria-expanded`(기본 false)와 `aria-controls`를 추가하고,
  각 답변 div에 대응 id(`faq-a-1`~`faq-a-7`)를 부여. `toggleFaq()` JS 함수가 클릭 시 이전에
  열려있던 버튼의 `aria-expanded`를 false로, 새로 열리는 버튼은 true로 갱신하도록 수정.
  스크린리더 사용자가 아코디언 열림/닫힘 상태를 인지할 수 있게 됨. 시각적 변화 없음.
- 검증 결과: 통과. verify_render.py 콘솔 에러 베이스라인과 동일(8건, CDN/폰트 오프라인 오류만).
- 커밋: `f5231c2`
- 참고: 백그라운드 에이전트가 이 작업 도중(HTML은 수정, JS 반영 직전) 응답 없음으로 중단되어
  사용자 복귀 후 나머지를 이어서 마무리함.

