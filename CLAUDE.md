# CLAUDE.md — 비즈페이지 온라인견적 프로젝트 상시 규칙

이 파일은 **변하지 않는 규칙**이다. 파일 역할표·검증 명령·배포 제약은 `README.md`,
지금 어디까지 했는지는 **`git log --oneline`**에 있다(커밋 메시지에 무엇을 왜 그렇게
고쳤는지, 어떤 함정이 있었는지가 들어 있다 — 그게 이 저장소의 기록 방식이다).

⚠ 예전엔 `RESUME_NOTE.md`라는 인수인계 문서가 있었지만 **2026-07-31 사용자 요청으로
삭제**했다(일회성 문서였고 역할이 끝났다). **다시 만들지 말 것** — 상태를 두 곳에 적으면
반드시 어긋나고, 이 저장소는 그 유형의 결함으로 이미 여러 번 당했다(결함 생성기 ①).
지운 내용은 git 이력에 그대로 있다.

## 프로젝트

MICE 기업연수 온라인 견적 제공. 정적 프론트(`index.html`/`script.js`/`data.js`/`admin.html`)
+ Vercel 서버리스 `api/` + Neon Postgres + bcrypt/JWT 인증.
프로덕션 `bizpage-estimate.vercel.app` — **`git push origin master` = 자동 배포**.

운영자는 사장님 1명 + 팀원 5명(매니저 1 + 직원 4). 팀원은 관리자 화면에서 요율 단가를
직접 갱신한다.

## 승인 범위 (2026-07-30 사용자 결정)

- **승인 없이 진행한다**: 결함 찾기, 코드 수정, 테스트·감사 도구 추가, 리팩터, 로컬 커밋.
  후보를 늘어놓고 고르라 하지 말고, 판단해서 진행하고 결과를 보고한다.
- **반드시 먼저 물어본다**: `git push`(=배포), 운영 DB 마이그레이션 실행,
  **도메인 값 판단**(가이드 일당·정산 통화·보험 단가 등 실거래가에 걸린 숫자).
  → 이 셋은 사장님이 모르는 사이에 고객이 보는 금액이 바뀌거나, 추정치가 사실로 굳는다.
- 수정은 커밋으로 남겨 되돌리기 쉽게 한다. 여러 건을 모아 배포 승인은 한 번에 받는다.

## 손대기 전에 돌릴 것 (읽기 전용, 1분)

```
node ai-loop/run_all_tests.js       # 회귀 스위트 — 계산식 바꿨으면 무조건 이것부터
node ai-loop/fuzz_invariants.js     # 견적 엔진 불변식 (--full = 52,360건)
node ai-loop/audit_consistency.js   # 목적지 목록 교차 정합성 (오류 0이어야 정상)
node ai-loop/audit_rates.js         # 요율 '값' 점검 — ⚠ 결과는 '확인 대상'이지 '오류'가 아니다
```

실행 시 `NODE_PATH="<프로젝트경로>/node_modules"` 필요(jsdom이 `--no-save`로 설치돼 있음).
⚠ **`npm install`을 돌리면 jsdom이 지워진다.** package.json에 없는 패키지라 npm이 정리해
버리고, 그러면 스위트가 통째로 크래시한다(42개 파일이 한 번에 죽어 봤다). 의존성을
건드린 뒤에는 **`npm install jsdom --no-save`를 다시 돌릴 것.**
**턴이 끝날 때 Stop 훅(`ai-loop/hooks/verify_on_stop.js`)이 앞의 두 개를 자동 실행**하지만,
그건 마지막 그물이다. 고치는 중에 직접 돌려서 빨리 알아내는 게 맞다.

## 결함 생성기 4가지 — 코드를 쓰기 전에 자문할 것

이 저장소에서 나온 결함은 거의 전부 아래 넷 중 하나다. 새 코드를 쓸 때마다 확인한다.

1. **목록이 여러 곳에 흩어져 하나를 빠뜨린다.**
   실제로 6번 터졌다(동유럽 통화·지역, PF 스냅샷/패널, PG 견적서 표시, PP 보험권역, PQ 시즌).
   → **목록을 두 번 적지 말 것.** `data.js`를 진실로 두고 서버 검증·관리자 폼·엔진이
   거기서 파생되게 한다. 불가피하게 나뉘면 테스트로 대조한다.

   **목적지 분류는 PY에서 구조적으로 정리됐다** — 좌석 구간·보험 권역·관리자 지역·
   정산 통화·시즌 달력·반구를 `data.js`의 **`DEST_CLASSIFY` 한 표**에 모았고
   `BIZ_ZONES`·`INSURANCE_ZONES`·`REGION_MAP`·`DEST_CURRENCY`·시즌 프로파일 `keys`·
   `SOUTHERN_HEMISPHERE_DESTS`가 전부 여기서 파생된다(`destGroupsBy`/`destFieldMap`/
   `destKeysWhere`). **목적지를 추가할 때 손댈 곳은 `destinationRates` 행 + `DEST_CLASSIFY`
   한 줄 + `index.html`의 select 세 곳뿐**이다. 분류표에 값이 비거나 모르는 구간명이면
   `DEST_CLASSIFY_ISSUES`에 기록되고 `audit_consistency.js`가 오류로 잡는다.
   `ai-loop/test_pY_single_source.js`가 55개 목적지의 6축 분류를 스냅샷으로 고정한다.

   아직 남은 이중 관리 두 가지는 의식할 것:
   - `index.html`의 `<select id="destination">` — optgroup·표기가 화면 구성이라 미파생.
   - 계수를 새로 추가하면 **엔진 + P6 스냅샷 + admin.html `coefContribHtml` 세 곳**을 함께 늘린다.

2. **폴백이 조용하다.**
   못 찾으면 중립값으로 떨어지고 콘솔 경고만 남는 코드가 반복해서 틀린 금액을 냈다.
   시즌 프로파일은 폴백이 '중립'도 아니라 **다른 계절**이었다(최대 36%, 부호까지 반대).
   → 폴백할 때는 **화면이나 감사 도구에 반드시 흔적을 남긴다.** 안전장치와 방치는 다르다.

3. **안전망이 실제로 실행된 적이 없다.**
   P13이 P10 테스트를 무효화했는데 아무도 안 돌려 깨진 채 방치됐고, PO의 `_verify`는
   저장만 하고 화면에 안 띄웠고, 감사기는 아무도 팔지 않는 정적 가격을 검사하고 있었다.
   → 안전망을 만들면 **일부러 망가진 입력을 넣어 잡히는지 확인**하고 그 확인을 테스트로 남긴다
   (`test_pQ_custom_dest_audit.js`가 그 예 — 가짜 응답을 `file://`로 흘린다).

4. **공개 입력을 신뢰한다.**
   `/api/quotes`·`/api/inquiries`·`/api/quote-shares` POST는 **인증 없이 누구나** 호출한다.
   여기서 온 값이 관리자 화면·고객 견적서에 그대로 렌더됐다(저장형 XSS, 실제 재현 확인).
   → 문자열은 `esc()`, **id는 `safeId()`**, 숫자 자리는 `Number()`.
   ⚠ **인라인 `onclick` 안의 JS 문자열은 `esc()`로 막을 수 없다**(홑따옴표를 안 바꾸고,
   `&#39;`로 바꿔도 HTML 파서가 되돌린 뒤 JS가 해석한다). 그 구조 자체를 피하는 게 안전하다.

## 반드시 지킬 것

- **`index.html` / `styles.css` / `script.js` 전체 재작성 절대 금지** — 섹션 단위 Edit만.
  (과거 전체 재작성이 토큰 한도로 잘려 스타일이 통째로 유실된 사고가 있었다.)
- **검증은 소스 읽기로 끝내지 말고 jsdom으로 실제 렌더**해 볼 것. XSS 건도 코드만 봤을 때는
  "esc 있으니 괜찮겠지"로 넘어갈 뻔했다.
  테스트는 `data.js` + `company-info.js` + **`rec_fallbacks.js`** + `script.js`를 **한 문자열로
  합쳐 한 번에 eval**해야 `const` 전역이 서로 보인다. `const`는 `window`에 안 붙으니 EXPOSE
  문자열로 노출한다. ⚠ `rec_fallbacks.js`를 빼면 `REC_FALLBACKS`가 undefined라 일정 탐색이
  그 자리에서 죽는다(RJ에서 실제로 겪었다). `ai-loop/test_rJ_rec_preview.js`가 `script.js`를
  합쳐 eval하는 픽스처 전부에 이 파일이 들어 있는지 검사하므로, 빠뜨리면 거기서 걸린다.
- **계산식을 바꿨으면 `run_all_tests.js` 전수 실행 + 금액 영향 실측**(변경 전/후 견적을
  직접 대조해 몇 %가 움직였는지 숫자로 말한다. "불변"이라고 추측하지 말 것).
- **요율의 진실은 `data.js`가 아니라 운영 DB(`rate_overrides`)다.** `data.js`는 폴백 기본값이라
  점점 낡는다. 요율 값 판단이 필요하면 `curl -s https://bizpage-estimate.vercel.app/api/rates`로
  실제 오버라이드를 먼저 확인한다.
- **추천 일정·추천 콘텐츠도 같은 구조다(QB·QC).** `data.js`의 `ITINERARY_DB`(코스)와
  `DEST_REC`(방식 A/B)는 기본값이고, 관리자 → 일정 관리에서 담당자가 저장한 값이
  `itinerary_overrides`(컬럼 `courses`·`rec`)에 얹혀 고객 견적서·일정 탐색에 나간다
  (`/api/content?action=itineraries`). 내용을 판단할 일이 있으면 **상수가 아니라 이 API를**
  먼저 본다. ⚠ `ITINERARY_DB`는 예전엔 `script.js`에 있었는데 **`data.js`로 옮겼다** —
  `admin.html`이 `data.js`는 싣지만 `script.js`(견적 엔진)는 싣지 않기 때문이다. 편집 화면용으로
  admin에 표를 복사하면 같은 목록이 두 벌이 된다(결함 생성기 ①).
  둘은 **한 행·한 번의 저장**으로 다룬다(테이블을 나누면 절반만 저장된 상태가 생긴다).
  `rec`은 `a`·`b` 둘 다 있어야 적용된다 — 한쪽만 저장되면 그 자리가 조용히 일반 문구로
  떨어진다(결함 생성기 ②). PUT에서 `rec`을 생략하면 기존 값을 건드리지 않는다(`coalesce`).
- **Vercel Hobby = 서버리스 함수 12개 제한에 이미 도달.** 새 API는 새 파일이 아니라 기존
  파일에 `?action=` 분기로 추가한다.
  ⚠ **`api/` 아래는 깊이와 무관하게 파일 하나가 함수 하나다** — `api/admin/login.js`도,
  `api/quotes/[id].js`도 한 칸씩 먹는다. 빠지는 것은 `_`로 시작하는 칸(`api/_lib/…`)뿐이다.
  2026-08-27 실측 **12/12** (맨 위 6 + `admin` 3 + `[id]` 3, 프로덕션 응답으로 전부 확인).
  🔴 그날 `audit_internals.js`가 맨 윗칸만 세어 **6/12로 보고**했고, 그대로 믿었으면
  이 줄을 「한도에 여유가 있다」로 고칠 뻔했다. **세는 자가 문서와 어긋나면 세는 자부터 의심한다.**
- **공유 페이로드(`script.js` `shareData`)를 건드릴 때 비공개 항목 유출 주의.**
  `rows: data.rows.filter(r => !r.muted)` 한 줄이 ENBT 수익·현지 수익금·여행자보험을
  고객에게서 가리는 유일한 방어선이다(`test_pG_quote_surfaces.js`가 회귀로 잡는다).
- **`estimate-view.html`의 `decodeShareData`를 되살리면 견적서 위조 경로가 다시 열린다.**
- **도메인 판단이 필요한 계수 변경은 GPT와 협의 후 진행**(사용자 요청).
  `cd ai-loop && python gpt_critique.py <프롬프트.txt> <응답.txt>`.
  **GPT 1라운드는 대체로 신뢰하지 말 것** — 앵커를 이중 인상하거나 자기모순을 낸 전례가 있다.
  구체적 숫자로 반박하는 2라운드를 거쳐야 수렴한다. 근거는 `ai-loop/p*_prompt*.txt`로 커밋한다.
- **계수 값이 실거래가에 걸린 문제는 GPT보다 사용자가 정확하다.** 추정치로 밀지 말고 물어본다.


## 화면 규칙 — 「보자마자 이해되는가」 (2026-08-27 대표 지시, XT)

> 「고객이든 관리자든, 화면을 봤을 때 바로바로 이해되는 구조로 레이아웃이든 모든 게
> 최적의 UI/UX를 갖췄으면 좋겠다.」

🔴 **취향으로 고치지 않는다.** 좋아졌는지 아무도 판단할 수 없게 되기 때문이다.
화면을 만들거나 고쳤으면 **재고 나서** 말한다:

```
node ai-loop/audit_ux.js                    # 화면 7개 · 관리자 탭 17개를 센다
node ai-loop/audit_customer_journey.js      # 고객 화면 버튼을 전부 눌러 본다
node ai-loop/audit_admin_journey.js         # 담당자 화면 버튼을 전부 눌러 본다
python ai-loop/check_quote_form_layout.py   # 폼에 도착했을 때 무엇이 보이나 (브라우저)
python ai-loop/check_contrast.py            # 안 읽히는 글자
```

### 새 화면·새 칸을 만들 때 지킬 것 (전부 `audit_ux.js`가 센다)

1. **화면이 무엇인지 말한다.** 보이는 `<h1>`이 **정확히 하나** + 한 문장 설명.
   관리자 탭은 `.page-head`(제목 + `.page-sub`) 형식을 쓴다.
2. **비어 있을 때 다음 행동을 말한다.** 「데이터가 없습니다」로 끝내지 않는다 —
   *무엇이* 없고 *무엇을 하면* 되는지. 목록 렌더러를 공용으로 쓸 때는
   **부르는 쪽이 문구를 준다**(렌더러는 무엇이 없는지 모른다).
   ⚠ 「검색 결과 없음」과 「아직 한 건도 없음」은 **다른 말**이다. 할 일이 정반대다.
3. **버튼 이름이 무슨 일이 날지 말한다.** 「확인」·「적용」처럼 무색한 이름을 쓰지 않는다.
   🔴 **고르는 버튼에 행동 이름을 붙이지 않는다** — 「📄 PDF 제출」이 실제로는 방식
   선택이라 **눌러 놓고 제출한 줄 아는** 자리였다. 고르는 것은 `aria-pressed`로 표시한다.
4. **낭독기가 읽을 이름을 준다.** `placeholder`는 이름이 아니다(값을 넣으면 사라진다).
   🔴 그리고 **이름이 같은 버튼을 무더기로 두지 않는다.** 「저장」이 110개, 「편집」이
   60개면 귀로는 구별이 안 된다 — 표는 줄의 이름을, 목록은 항목 이름을 `aria-label`에 넣는다.
5. **영문·기술 용어를 화면에 내보내지 않는다.** `golfRounds`가 고객 안내 문구로 나간 적이 있다.
6. **한 자리에 강조 버튼을 둘 두지 않는다.** 무엇부터 눌러야 할지 모른다.
7. **긴 폼은 지금 어디인지 말한다.** 「1 / 2 단계」 + 「필수는 몇 칸」.
   폼이 화면보다 길면 고객은 **끝이 어딘지 모른 채** 스크롤한다.

### ⚠ 재는 자가 틀리면 고칠 것을 못 찾는다 — 실제로 열 번 넘게 겪었다

- 행동어에 **명사 「문의」**를 넣었더니 「**문의** 내역이 없습니다」가 통과했다 —
  자가 스스로 대시보드 첫 화면의 결함을 가려 줬다.
- **카드 제목을 화면 제목으로** 세어 「제목이 있다」고 읽혔는데, 17개 탭 어디에도
  화면 제목이 없었다.
- 문서 전체를 눌러 **같은 버튼을 탭 17번씩** 셌다(1,361개 → 166개).
- 「Not implemented: navigation」을 **터진 버튼 51개**로 셌다 — jsdom 한계지 결함이 아니다.
- 같은 안내가 이미 떠 있어 **말을 하고 있는 버튼**을 「조용하다」로 셌다.
- 「첫 화면에 다음 버튼이 보이는가」로 쟀더니 **늘 ✗**였다. 늘 ✗인 잣대는 아무것도 안
  말하고, 사람은 곧 그 줄을 안 읽는다.

🔴 **숫자를 0으로 만들려고 자를 깎지 말 것.** 결함이 아니라고 판단했으면 **그렇게 적고
남긴다**(설정 탭의 강조 버튼 2개가 그런 건이다). 깎으면 다음에 진짜가 와도 안 걸린다.

### ⚠ jsdom이 못 보는 것

스타일시트로 감춘 것(`.tab-panel { display:none }`)은 **보이는 것처럼 읽힌다.**
`audit_ux.js`의 `CSS로감춘것` 표에 규칙을 적어 두었다 — 새 화면에 같은 방식이 생기면
거기 한 줄 늘린다. **진짜 보이는지는 브라우저로만 안다**(`check_*_layout.py`).

## 알아두면 헷갈리지 않는 것

- `sightseeing_fee` = **1인당 여행 전체 일정의 관광비 묶음**이고 **4~5일 기준**. 1일치가 아니다.
  (일수 정비례로 바꾸면 4~7배로 뛴다. 장가계 260,000·뉴욕 250,000이 그 근거.)
- `vipCount` = **"1인 1실 임원 인원"이라 좌석 등급과 무관**하고, 객실 구성을 '혼합'으로
  골라야 화면에 보인다. 좌석 관련 기능에서 재사용하지 말 것(PD에서 별도 `bizCount` 신설).
- ⚠ **DB의 bigserial `id`는 JS에서 문자열이다**(`staff_accounts.id === "1"`). JWT `sub`도,
  `req.user.id`도, API가 화면에 주는 `id`도 전부 문자열이다. `Number(id) === req.user.id`
  같은 비교는 **항상 false**라 조용히 무력해진다 — QE에서 사장님 자기보호 가드가 정확히
  그래서 한 번도 발동하지 않았다. id 비교는 `String(a) === String(b)`로 한다.
- `audit_rates.js`에 나오는 건 **'확인 대상'이지 '오류'가 아니다.** 장가계 관광비가 권역
  중앙값의 5배인 건 실제로 입장료가 비싸서다 — 중앙값으로 맞추면 수정이 아니라 데이터 파괴다.
- **실측 데이터가 프로덕션 DB에 0건**이라 측정 기반 작업(P5 신뢰구간 실동작·P6 역검증·
  실측 튜닝)은 계약완료 견적이 쌓여야 의미 있다.
- 스키마 변경은 `node ai-loop/db_migrate.js`(additive, `if not exists`). `DATABASE_URL`은
  `.env.local`. **실행은 사용자 승인 후.** API가 새 컬럼에 INSERT하면 **마이그레이션을
  배포보다 먼저** 해야 한다(순서가 뒤바뀌면 그 기능이 500으로 깨진다).
- ⚠ **`ai-loop/fx_seed.js`는 기본이 '없는 것만' 채우기다. `--force-reseed`를 함부로 쓰지 말 것.**
  예전엔 무조건 덮어써서, 누락 1건을 채우려 돌리면 나머지 전 목적지의 환율 기준선이 오늘
  환율로 리셋됐다(기준선 = 현재 환율이면 `getFxAdjust`가 전부 1.0이 되어 환율 변동이 견적에서
  통째로 사라진다). 누락분은 오늘 환율이 아니라 **같은 통화 동료의 기존 기준선을 복사**한다.
  `--dry-run`이 있으니 먼저 확인할 것.
