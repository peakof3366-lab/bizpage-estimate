/* ═══════════════════════════════════════════════════════════════════════════
   운영 상수 한 곳 (QO) — 서버 검증·관리자 화면 안내·운영 매뉴얼이 **같은 값**을 본다.

   왜 파일을 따로 뒀는가: 이 숫자들은 원래 서버(api/*.js)·화면(admin.html)·문서
   (manual.html) 세 군데에 각각 적혀 있었다. 그리고 실제로 어긋났다 —
   비밀번호 변경 화면이 "6자 이상"이라고 안내하는 동안 클라이언트도 서버도 8자를
   요구해서, 담당자가 규칙대로 넣었는데 거절당했다(커밋 87b6d70). 목록이든 숫자든
   두 번 적으면 반드시 어긋난다는 이 저장소의 결함 생성기 ① 그대로다.

   그래서 값을 아는 곳은 여기 하나이고, 나머지는 전부 여기서 읽어 간다.
   - 서버:   require('../limits') / require('../../limits')
   - 브라우저: <script src="limits.js"> → 전역 LIMITS
   - 매뉴얼:  data-fact="키" 로 표시하면 로드 시 이 값이 채워진다

   ⚠ 여기 값을 바꾸면 화면 안내와 매뉴얼 문구가 **자동으로 따라간다.** 대신 그 숫자를
   설명하는 산문(예: "브루트포스를 막기 위해")까지 따라가지는 않으니, 성격이 달라지는
   변경이면 manual.html의 설명도 함께 본다. ai-loop/test_qN_manual.js가 매뉴얼이 이
   값들을 실제로 렌더하는지 확인하므로, 키 이름을 바꾸면 거기서 걸린다.
   ═══════════════════════════════════════════════════════════════════════════ */
const LIMITS = {
  /* 로그인 세션 유지 시간. 늘리면 퇴사자 계정을 꺼도 그만큼 오래 살아 있다
     (api/_lib/auth.js는 요청마다 DB로 계정 상태를 다시 보므로 즉시 끊기지만,
     그 방어선이 없다면 이 값이 곧 노출 시간이다). */
  SESSION_HOURS: 12,

  /* 브루트포스 락아웃 — 이 횟수만큼 틀리면 아래 분(分)만큼 잠근다. */
  LOGIN_MAX_ATTEMPTS: 5,
  LOGIN_LOCKOUT_MINUTES: 15,

  /* 비밀번호 최소 길이. 서버 검증 3곳(변경·직원생성·재설정)이 전부 이 값을 쓴다. */
  PASSWORD_MIN_LENGTH: 8,

  /* 직원 가입코드 길이. 정규식도 이 값으로 만든다 — 숫자와 정규식이 따로 놀면
     화면 안내와 실제 검증이 어긋난다. */
  SIGNUP_CODE_MIN: 6,
  SIGNUP_CODE_MAX: 40,

  /* 목적지 하나에 둘 수 있는 추천 코스 개수 (api/content.js 저장 검증). */
  MAX_COURSES: 6,

  /* 코스 하나에 둘 수 있는 일자 개수. 서버 검증(api/content.js)과 편집 화면의 사전
     안내가 같은 값을 봐야 한다 — 화면이 막지 않고 서버만 거절하면, 담당자는 30개를
     넘겨 채워 넣은 뒤 저장 버튼을 눌러서야 그 사실을 안다(RD). */
  MAX_DAYS: 30,
};

if (typeof module !== 'undefined' && module.exports) module.exports = LIMITS;
