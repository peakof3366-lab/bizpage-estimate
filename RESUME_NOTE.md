# 🔖 작업 재개 노트 (다른 PC에서 이어가기용)

> 이 파일은 컴퓨터를 옮겨 작업을 이어갈 때 Claude에게 현재 상황을 알려주기 위한
> **일회성 인수인계 메모**입니다. 민감정보(비밀번호·계정·DB주소)는 담지 않습니다.
> 마지막 갱신: 2026-07-30 (PV까지)

## ▶ 다시 시작할 때 첫 스텝 (이 순서대로)

1. 저장소 폴더에서 **`git pull` 먼저**. 최신 커밋은 `git log --oneline -1`로 확인.
2. Claude Code를 열고: **"RESUME_NOTE.md 읽고 비즈페이지 작업 이어서 진행해줘"**
3. 손대기 전에 세 가지를 먼저 돌린다(전부 읽기 전용, 1분이면 끝난다):
   - `node ai-loop/run_all_tests.js` — 회귀 스위트 (현재 **932 pass / 0 fail · 29개 파일**)
     ※ 2026-07-30부터 **턴 종료 시 Stop 훅이 이걸 자동 실행**한다(견적 로직이 바뀐 턴만).
       그래도 고치는 중에는 직접 돌려서 빨리 알아내는 게 맞다.
   - `node ai-loop/fuzz_invariants.js` — 견적 엔진 불변식 (샘플 3,080건 / `--full` 52,360건)
   - `node ai-loop/audit_consistency.js` — 목적지 목록 교차 정합성 (현재 오류 0).
     **2026-07-30부터 라이브(/api/rates)가 기본**이라 커스텀 목적지도 검사한다(`--static`으로 정적 강제).
   ※ `audit_rates.js`는 값 판단이라 '확인 대상'만 나온다. 오류가 아니다.

## ▶ 현재 위치 (2026-07-30 기준)

- 견적 정확도 로드맵 **P1~P13 + P2b + PB~PH, PJ~PV 전부 프로덕션 라이브**
  (PT·PU·PV는 2026-07-30 배포, 프로덕션에서 `leadWrite` 존재 확인).
- 워킹트리 깨끗(개인 메모 txt만 untracked — 커밋하지 말 것).
- ⚠ **미푸시 커밋 = 배포 승인 대기**: `409327b`(PW 세션 무효화) + 이 노트.
  계산식 변경 없음(요율·견적 금액 불변) — 관리자 인증·화면만 바뀐다. 마이그레이션 불필요.
  운영 계정 3건 전부 active이고 `updated_at`이 8~9일 전이라 **이 배포로 로그아웃되는
  사람은 없다**(확인함).
- 프로덕션: `bizpage-estimate.vercel.app` (git push origin master = 자동 배포).
- 운영 DB 마이그레이션 3건 반영 완료(`staff_accounts.self_signup`,
  `custom_destinations.insurance_zone`, `custom_destinations.season_profile`).
  추가 마이그레이션 대기 없음.
- **팀원 온보딩은 아직 시작 전** — 2026-07-30 프로덕션 확인 결과 가입 신청이
  `signup_disabled`(가입코드 미설정 = 가입 기능 꺼짐). 아래 🔴 1번이 그대로 남아 있다.

## ▶ 🔴 이어서 할 일 (우선순위 순 — 2026-07-30 기준 그대로 남아 있음)

**1. 팀원 온보딩 — 사장님만 할 수 있는 것. 이게 제일 급하다.**
   - 관리자 → 설정 → 직원 계정 관리 → **직원 가입코드 설정**(영문·숫자 6~40자).
     지금은 코드가 비어 있어 **가입 기능이 꺼져 있다**(2026-07-30 프로덕션 재확인:
     가입 신청 시 403 `signup_disabled`).
   - 팀원에게 코드 + `bizpage-estimate.vercel.app/admin.html` 전달
   - 올라온 신청 **승인**(목록 맨 위 ⏳ 승인 대기) → 팀장 1명만 **매니저**로 승급
   - 다섯 명 다 들어오면 **코드를 비워 가입을 닫는다**
   - 팀원용 가이드는 Claude Artifact로 발행돼 있다(사장님 계정 → 공유 메뉴에서 열어주면 됨)

**2. 사장님 답이 있어야 반영되는 값 3건** (물어만 보면 반영은 금방이다)
   - **발리 가이드 일당** (현재 99,000 — 동남아 최저가인데 발리 식비는 아시아 최고가라 안 맞음)
   - **괌 / 사이판 가이드 일당** (현재 405,000 / 210,000 — 나머지 8항목은 쌍둥이인데 이것만 1.93배)
   - **랜드사 정산 통화** — 다낭·나트랑·푸꾸옥(VND)·캄보디아(KHR)·카자흐스탄(KZT)의
     지상비가 **USD로 입력돼 있는데 환율 보정은 현지 통화를 따라간다.** 달러 결제면
     `dest_currency.js` 매핑을 USD로 바꿔야 한다.

**3. PW 배포 승인** — 위 '현재 위치' 참고. 팀원이 시작하기 전에 나가야 의미가 있다
   (내보내기·강등이 실제로 일어나는 건 그때부터).

**4. 확정 과제 없음.** 위 3건이 정리되면 자연 정지점이다. 새로 착수할 거면
   "남은 아이디어" 절을 보고 **후보 제시 → 사장님 확인 후** 진행할 것.
   (2026-07-30에 PQ·PW를 이 방식으로 골라 진행했다. 다음 후보 1순위는 아래
   `admin-quote.html` 미점검, 그다음이 '결함 생성기 ①의 구조적 제거'.)

## ▶ 2026-07-30 — 팀 첫날 경로 점검 (PS·PT·PU·PV·PW)

다음 주부터 팀원 5명이 요율을 갱신하고 리드를 나눠 맡는다. **"1명일 땐 안 보이는데
5명이 되면 매일 밟는" 경로**만 골라 훑었고 네 건이 나왔다. 성격이 하나로 모인다 —
**공유 데이터를 자기 브라우저 사본으로 덮어쓰거나, 실패를 사람에게 알리지 않는다.**

**PS 되돌리기가 남이 바꾼 값을 말없이 덮어쓰던 것** (`f641e61`, 배포 완료)
- 되돌리기는 팀이 가장 의지할 안전망인데 **테스트가 하나도 없었다.** 확인창의
  '현재 값'이 실제로는 '이 이력이 넣은 값'이라 **거짓 정보**였다. 실제 현재 값과
  대조해 충돌을 나란히 보여주되 막지는 않는다(알고도 되돌릴 정당한 경우가 있다).

**PT 변경 이력이 300건을 넘으면 되돌리기가 조용히 닫히던 것** (`b8d4af7`, ⚠ 미푸시)
- 편집창 이력이 **전역** 최근 300건을 받아 destKey로 걸렀다 → 오래 편집된 목적지는
  이력이 통째로 사라지고 "변경 이력이 없습니다"가 뜬다('변경이 없었다'와 구별 안 됨).
- 서버에 목적지별 조회(`?history=1&destinationKey=`) 추가, 상한 도달 시 안내,
  **되돌리기를 위치(idx) → 행 id로** 바꿨다(캐시 둘로 나뉘면 idx는 엉뚱한 항목을 되돌린다).

**PU 담당자 지정이 동료의 상태·메모를 덮어쓰던 것** (`82ab827`, ⚠ 미푸시)
- 서버 PATCH가 네 필드를 통째로 쓰고 빠진 필드를 기본값으로 **초기화**했는데, 화면은
  나머지 값을 **자기 브라우저 localStorage 사본**에서 읽어 함께 보냈다. 담당자를
  지정하거나 문의를 **열어보기만 해도** 동료의 변경이 사라졌다. "전체 읽음" 한 번이
  가장 광범위하게 덮어썼다. → 서버 `coalesce(보낸값, 기존값)` + 화면은 바꾼 필드만 전송.
  ⚠ `?? null`이 아니라 undefined만 null로 — 빈 문자열은 '지우기'라는 유효한 값이다.

**PV 관리자 화면의 저장 실패가 조용히 사라지던 것** (`d609983`, ⚠ 미푸시)
- **PR에서 고친 고객 제출 경로의 결함이 관리자 화면에 그대로 남아 있었다**(당시 점검이
  공개 화면만 봤다). 리드 쓰기 네 헬퍼가 `fetch(...).catch()`만 써서 **401·403·5xx는
  reject하지 않으므로** 콘솔 경고조차 없었다.
- ⚠ 주석은 "실패해도 로컬 캐시는 이미 갱신된 상태"라며 안심시켰지만 **캐시는 안전망이
  아니다** — `loadRemoteData()`가 다음 동기화에서 캐시를 서버 값으로 통째로 덮어쓴다.
  담당자는 처리했다고 믿고, 팀원 화면엔 미처리로 남고, 다음 로그인에 흔적 없이 사라진다.
  세션이 12시간이라 탭을 열어둔 채 만료되면 이 상태가 된다.
- 답변 확정·진행 기록은 더 나빴다 — 실패해도 직접 만든 기록을 렌더해서 **서버에 없는
  답변이 "○○ · 7/30 답변"으로 화면에 찍혔다.**
- `leadWrite()`로 통일(res.ok 확인 → 재시도 → 실패 종류별 안내 → 서버 값 재동기화),
  **성공한 뒤에 화면을 바꾸는 순서**로 뒤집었다(실패 시 모달을 닫지 않아 쓴 메모를 잃지 않음).
- ⚠ **재시도는 멱등한 쓰기에만.** 진행 기록(addLog)은 서버가 덧붙이는 연산이라
  재시도하면 기록이 두 줄 남는다 → `retries: 0`. PR의 재전송 근거가
  `on conflict do nothing`이었던 것과 같은 판단이다.
- 곁다리: 개별 삭제 버튼은 직원에게 숨겼는데 **일괄 삭제 셋(문의 전체·견적 선택·견적
  전체)이 빠져 있었다** — 지우는 범위가 더 넓은 쪽이 빠져 있었다. 역할 숨김에 추가.

**PW 비활성화·강등이 12시간 반영되지 않던 것** (`409327b`, ⚠ 미푸시)
- PV 점검 중 발견 → 사장님이 '요청마다 DB 확인' 방식을 선택해 착수.
- `requireAdmin`이 **JWT만 검증하고 DB를 보지 않았다.** 세션 12시간이라 ①퇴사자 계정을
  비활성화해도 열어둔 탭에서 12시간 사용 가능 ②role이 토큰에 박혀 **매니저→직원 강등이
  12시간 무효**(목적지 삭제·일괄조정 계속 가능) ③비밀번호 재설정이 기존 세션을 못 끊음.
- → 요청마다 `staff_accounts` 한 행 확인. **role·displayName을 DB 값으로 쓴다**(강등 즉시
  반영의 핵심). 거부 시 401 + 쿠키 삭제. `updated_at > iat`면 세션 종료.
- ⚠ **`staff_accounts.updated_at`을 만지는 것은 곧 "그 계정의 세션을 끊는다"는 뜻이다.**
  마이그레이션 없이 무효화 근거를 얻은 대가다. 이 컬럼을 갱신하는 코드를 추가할 때 의식할 것.
  로그인은 이 컬럼을 안 건드린다 — 건드리면 자기 토큰을 무효화해 로그인 직후 튕긴다.
- iat가 초 단위로 내림되므로 **1초 여유**를 뒀다(없으면 승인 직후 로그인한 사람이 튕긴다).
- DB 조회 실패는 **통과시키지 않고** 503 `session_check_failed`로 구별한다(401 '만료'로
  말하면 담당자가 재로그인을 반복한다). 대가: 관리자 API 호출마다 DB 쿼리 1회 추가.
- 곁다리로 **503이 깨우는 옛 구멍**을 막았다 — `loadRemoteData`가 401만 보고 res.ok를
  확인하지 않아 500·503이면 `{error:...}` 객체가 목록 캐시에 들어가 렌더가 터지고
  캐시까지 오염됐다(새로고침해도 안 낫는다). res.ok·배열 확인 + 실패 시 캐시 유지.

전체 **932 pass / 0 fail · 29개 파일**. PV·PW 테스트는 각각 **옛 동작을 일부러 심어**
28곳·10곳이 잡히는 것까지 확인했다(안전망이 실제로 도는지 확인 — 이 저장소 관례).

## ▶ 2026-07-30 후반 — 자동화·노출 차단·리드 유실 (배포 완료)

아래 4건은 배포됐다. (CLAUDE.md는 보안 방어선을 한곳에 모은 파일이라 노출 차단과
함께 나가야 했다 — 그렇게 배포했다.)

**턴 종료 시 자동 검증 + CLAUDE.md** (`63b00db`, `46ab52e`)
- 결함이 반복되는 원인이 "안전망은 있는데 사람이 기억해서 돌려야 한다"였다. Stop 훅
  (`ai-loop/hooks/verify_on_stop.js`)이 견적 로직이 바뀐 턴 종료 시 회귀 스위트·정합성
  감사를 자동 실행하고 실패하면 턴을 차단한다. 변경 판단은 **미커밋 + 미푸시 커밋** 기준
  (워킹트리만 보면 "커밋했지만 안 돌린" 경우를 놓치고, push되면 조용해져야 한다).
- `CLAUDE.md` 신설 — 세션마다 자동 로드되는 상시 규칙. **결함 생성기 4가지**(목록 산포·
  조용한 폴백·실행된 적 없는 안전망·공개 입력 신뢰)와 **승인 범위**를 명문화.
  이 파일이 '규칙', 이 노트가 '상황'이다. 어긋나면 CLAUDE.md가 우선.

**내부 문서가 프로덕션에서 공개로 읽히고 있었다** (`2906b84`)
- 루트가 정적 출력이라 커밋한 파일이 그대로 공개된다. 실측: `/RESUME_NOTE.md` 200,
  `/ai-loop/db_migrate.js` 200(DB 전체 스키마), `/ai-loop/test_pJ_public_input.js` 200
  (어떤 공격이 막히고 무엇이 안 막히는지), `/.claude/settings.local.json` 200.
- 자격증명은 없었다(`.env*`·`api/_lib` 404, `api/*.js`는 정적 파일이 아니라 함수로
  라우팅 — `/api/rates.js`는 소스가 아니라 API 응답을 준다). 그래도 지도가 된다.
  특히 이 노트가 견적서 검증의 한계를 적어둔 문장은 우회법을 알려주는 것이다.
- `.vercelignore`로 문서·검증 도구·에이전트 설정을 배포에서 제외. 서빙 규칙에 기대지
  않고 업로드 자체를 막았다(README.md는 404인데 RESUME_NOTE.md는 200이라 .md 서빙
  여부가 일정하지 않다). **⚠ 이 노트에 뭘 적든 배포 전까지는 여전히 공개다.**

**PR 리드 유실 방지** (`5ca6d88`)
- 세 제출 경로가 `fetch(...).catch(...)`만 썼다. **fetch는 500에 reject하지 않으므로
  서버 오류가 성공으로 처리**됐고, 고객에게는 언제나 "접수 완료"가 떴다. 실패한 리드는
  고객이 연락을 기다리고 회사는 존재조차 모른다.
- "로컬에는 저장됨" 주석이 안전망처럼 읽혔지만 localStorage는 **고객 브라우저**라
  담당자는 볼 수 없다. 회사 쪽 복구 수단이 아니었다.
- `submitLead()`로 통일: res.ok 확인 → 5xx·네트워크는 백오프 재시도 → 대기열 보관 →
  다음 방문·`online` 이벤트에 자동 재전송 → 최종 실패 시 전화·이메일 직접 연락 안내.
- 4xx는 재시도 안 함(429 제외), 본문 60KB 미만은 `keepalive`(탭 닫힘 대비),
  실패 안내는 자동으로 안 사라지고 상담 폼도 닫지 않는다.
- ⚠ **재시도가 안전한 근거는 두 API의 `on conflict (id) do nothing`이다.**
  이게 깨지면 재시도가 리드를 복제한다 — `test_pR_lead_retry.js`가 회귀로 검사한다.
- 알려진 한계: `admin-quote.html`(내부 도구)은 저장 실패를 담당자에게 안 띄운다
  (대기열 재전송으로 데이터는 남는다).
  ⚠ **당시 점검이 공개 화면만 봤다** — `admin.html`의 리드 쓰기에 같은 결함이 그대로
  남아 있었고 **2026-07-30 PV에서 고쳤다.** `admin-quote.html`은 여전히 미점검이다.

전체 **742 pass / 0 fail · 24개 파일**(당시 기준).

## ▶ 2026-07-30 — PQ 커스텀 목적지 시즌 프로파일 (라이브)

**커스텀 목적지가 DEST_SEASON_PROFILES에 편입되지 않았다** (`49365b1`)
- PP에서 고친 보험 권역과 **같은 유형**인데 폴백이 '중립값'이 아니라 **'다른 계절'**이라
  더 크게 어긋난다. 동남아 목적지를 추가하고 7월에 출발하면 공용표는 성수기 1.20,
  실제 동남아는 우기 비수기 0.88 — 항공·유류·호텔이 **36% 어긋나고 부호까지 반대**다
  (과청구 방향). 커스텀 목적지 0건이라 안 터졌을 뿐, 매니저가 하나 추가하는 순간이었다.
- DEST_SEASON_PROFILES 각 항목에 `id`·`name` 신설(매칭은 그대로 keys 기반 → 내장 동작 불변).
  DB 컬럼 → 관리자 폼 → 서버 검증 → GET → 엔진 편입 다섯 곳 연결(PP와 같은 구조).
- **PP의 '세 곳 동기화' 부담을 늘리지 않았다** — 서버 허용 키는 data.js가 내보낸
  DEST_SEASON_PROFILES에서 뽑고, 관리자 폼 선택지도 같은 배열로 만든다. 프로파일을
  추가하면 서버·폼에 자동 반영된다(하드코딩하면 폼에만 없어 고를 수 없고 조용히 폴백).
- 값이 없거나 모르는 id면 종전 폴백이라 **마이그레이션만으로는 금액 불변** —
  HEAD 대비 15목적지 × 6출발일 × 3(인원/일수) = **270건 항목 단위 대조, 차이 0건**.
- 저장 직전 confirm에 어떤 시즌 달력으로 계산되는지 표시. 빈 값은 '중립'이 아니라
  공용표라는 사실을 그 자리에서 알린다(막지는 않는다 — 맞는 프로파일이 없는 목적지도 있다).

**audit_consistency.js를 라이브로 전환** (같은 커밋) — 어제 노트의 ⚠ 숙제를 해결한 것
- 정적값만 봤기 때문에 매니저가 목적지를 추가하면 그 목적지는 **감사기 시야 밖**이었다.
- ⚠ **커스텀 목적지를 정적 목록(BIZ_ZONES 등)과 대조하면 안 된다** — 정적 목록에 없는 게
  정상이고(런타임 편입) 그렇게 짜면 목적지 하나 추가에 '오류 6건'이 떠서 감사기를 못 쓴다.
  그래서 커스텀은 **DB 행의 분류값이 채워져 있는가**를 본다. 지역 분류로 기대 프로파일을
  알 수 있으면(`REGION_SEASON_EXPECT`) 불일치까지 객관적 오류로 잡는다.
- 지역·통화 누락은 **참고**(의도적으로 안 쓸 수 있다), 좌석·보험·시즌 키 오류는 **오류**.
- 값 형식 검사도 라이브 병합값 기준으로 바꿨다 — 오버라이드·커스텀의 잘못된 값은
  정적표에 안 나타나 예전엔 통과했다.
- **`ai-loop/test_pQ_custom_dest_audit.js` 신설(27 pass)** — 커스텀 0건이라 이 새 절은
  *한 번도 실행된 적 없는 코드*였다. `BIZPAGE_RATES_URL`을 `file://` 가짜 응답으로
  바꿔 감사기를 돌려, 일부러 망가진 목적지 4종이 실제로 잡히는지 + 정상 목적지에
  거짓 경보가 없는지를 확인한다. 운영 DB·네트워크를 건드리지 않는다.
- test_pP에도 **종단 검증** 추가 — fetch를 가짜 /api/rates 응답으로 바꿔
  `applyRateOverrides`를 실제로 돌린다(편입 로직 '재현'이 아니라 실제 코드 경로).
  오버라이드가 커스텀 목적지보다 먼저 적용되면 편집분이 사라지는 순서 의존도 함께 잡는다.

전체 **707 pass / 0 fail · 23개 파일** · 퍼저 52,360건 위반 0 · 교차 정합성 오류 0.

## ▶ 이번(2026-07-29) 세션 요약 — 자세한 건 아래 각 절

하루에 **11커밋**. 성격이 셋으로 나뉜다.
- **보안 3건** — 관리자 저장형 XSS(치명, 재현 확인), 고객 견적서 일차번호 주입,
  견적서 링크 위조 경로(`?d=`) 제거
- **정확도 2건** — 음력 연휴가 엉뚱한 날에 붙던 것(라이브에서 실제로 틀린 금액이
  나가던 중이었다), 커스텀 목적지 보험 권역 누락
- **팀 운영 6건** — 감사기 라이브 전환, 요율 오타 서버 상한, 동시 편집 변경 유실,
  삭제 권한, 직원 자가 가입, 검증 결과 가시화
테스트는 14개 파일 374건 → **22개 파일 656건**으로 늘었다.

## ▶ 2026-07-28 세션에 한 것 — PB · PC · PD (전부 라이브)

**PB 여행자보험 권역·기간 차등** (`e0e30d9`)
- 목적지·기간 무관 1인 15,000원 정액 → `INSURANCE_BASE(18,000) × zoneFactor × durationFactor`.
  기준단가는 15,000이 실거래 대비 과소라는 사용자 판단으로 18,000 상향.
- 좌석등급용 `BIZ_ZONES`를 재사용하지 않고 **보험 전용 `INSURANCE_ZONES` 신설**
  (BIZ_ZONES는 노선 거리 기준이라 괌·사이판·호주·몽골에서 보험 기준과 어긋남).
  asiaShort 0.85 / asiaMid 1.00(기준) / evac 1.20 / oceania 1.50 / highCost 1.80
- 일수: 1~3일 0.80 / 4~5일 1.00(기준) / 6~7일 1.20 / 8~10일 1.40 / 11~15일 1.70 / 16일+ 2.00

**PC 관광비 일수 계수** (`0073689`)
- `sightseeing_fee`는 **1인당 전체 일정 묶음이고 4~5일 기준**(사용자 확정). 일수 미반영이라
  파리 3일과 10일이 같은 금액이던 것을 체감형 계수로 보정. **일수 정비례가 아님**(정비례로
  바꾸면 4~7배로 뛴다 — MICE는 기업방문이 핵심, 장거리엔 이동일이 낀다. 탄력성 0.6).
  1~2일 0.75 / 3~5일 1.00(기준) / 6~7일 1.30 / 8~10일 1.60 / 11~15일 1.95 / 16일+ 2.10

**PD 좌석 등급 '혼합'** (`77c7ad4`)
- 좌석이 전원 일괄이라 "임원 2명만 비즈니스"를 표현 못 하던 것 → '혼합' 옵션 + `bizCount`
  입력칸 신설. `bizFactor = (bizCount × 노선배율 + 나머지) / 총원` 가중평균이라 이후
  볼륨 할인(tieredTotal)이 기존 그대로 동작하고, 양 끝단(0명/전원)에서 기존 두 경로와 금액 일치.
- **`vipCount` 재사용 안 함** — vipCount는 "1인 1실 임원"이라 좌석과 독립이고, 객실 구성을
  '혼합'으로 골라야 보이는 필드라 재사용했으면 2인 1실 견적에서 조용히 무시됐을 것.

공통: 보험·관광은 '원가'라 계수 노브·P11 변동성 상한 대상 아님(의도된 설계).
검증 전체 스위트 **273 pass / 0 fail**. 근거 `ai-loop/pB_*.txt`·`pC_*.txt`(각 GPT 2라운드).

## ▶ 감사 도구 (2026-07-28 신설) — 손대기 전에 먼저 돌릴 것

- `node ai-loop/run_all_tests.js` — **계산식을 바꿨으면 무조건 이거 먼저.**
  test_p*.js 전수 실행·집계. 실패/크래시는 exit 1. 현재 309 pass / 12파일 / 약 15초.
- `node ai-loop/audit_consistency.js` — 목적지가 7개 목록(요율표·index select·BIZ_ZONES·
  INSURANCE_ZONES·REGION_MAP·DEST_CURRENCY·시즌 프로파일)에 일관되게 있는지 대조.
  **여기서 걸리는 건 전부 객관적 오류**(exit 1). '참고'는 따로 분리돼 exit code에 안 들어감.
  → **목적지를 추가/삭제하면 반드시 이걸 돌릴 것.** 한 목록만 빠뜨리면 금액이 조용히 틀어진다.
- `node ai-loop/audit_rates.js` — 요율 '값'의 내부 정합성(권역 내 이상치·비율·라운드 이탈 등).
  ⚠ 여기 나오는 건 **'확인 대상'이지 '오류'가 아니다.** 장가계 관광비가 권역 중앙값의 5배인 건
  실제로 입장료가 비싸서다. 중앙값으로 맞추면 수정이 아니라 데이터 파괴다.

## ▶ 2026-07-28 후반 — 감사에서 나온 실제 오류 수정 (전부 라이브)

- **동유럽 환율 보정 누락** (`ae19650`) — DEST_CURRENCY에 동유럽만 빠져 있어
  `getFxAdjust('동유럽')`이 항상 1.0이었다(프로덕션 55개 중 54개만 기준선 보유).
  → EUR 매핑 + 운영 DB에 기준선 1행 삽입(서유럽 EUR 1701.547 @2026-07-20 복사).
  **운영 DB 반영 완료·검증 완료(55/55).**
- **동유럽이 REGION_MAP에서 '중앙아시아'** (`ae19650`) — 요율은 유럽 티어인데 그룹만
  중앙아시아라 지역별 일괄조정에서 유럽에는 빠지고 중앙아시아에 잘못 딸려갔다. → '유럽'.
- **fx_seed.js 재실행이 파괴적이던 문제** (`ae19650`) — 기존엔 무조건 덮어쓰기라,
  누락 1건 채우려고 돌리면 나머지 54건 기준선이 오늘 환율로 리셋돼 환율 변동이 통째로
  사라진다. 기본을 '없는 것만'으로 바꾸고 `--force-reseed`로 분리. 누락분은 오늘 환율이
  아니라 **같은 통화 동료의 기존 기준선을 복사**한다(안 그러면 그 목적지만 adjust=1.0).
- **PE 차량 자동선택 임계** (`3befff1`) — 소형 정원 25명인데 10명부터 대형이 잡히던
  코드×데이터 충돌. `VEHICLE_CAPACITY.small`에서 파생하도록 변경(매직넘버 제거).
  10~25명 1인 총액 **-1.8% ~ -4.5%**. 9명 이하·26명 이상 무변경.
- **PF 새 계수가 스냅샷·패널에서 누락** (`6eff34d`) — PB·PC 계수를 엔진에만 넣고,
  그 계수를 기록/표시하라고 만든 두 경로를 확장하지 않았던 것. P6 역검증 스냅샷에
  `insuranceInfo`·`sightDuration` 추가, 관리자 계수 기여도 패널에 관광·보험 행 추가.

  ⚠ **계수를 새로 추가하면 세 곳을 같이 늘릴 것** — ①엔진(script.js) ②P6 스냅샷
  ③admin.html `coefContribHtml`. 하나라도 빠지면 나중에 그 견적이 어떤 배율을 썼는지
  복원할 수 없고(스냅샷), 내부 검토자가 계수 일부만 보고 판단한다(패널).
  `ai-loop/test_pF_snapshot_panel.js`가 이 세 곳의 동기화를 검사한다.
- **PG 조건이 견적서·관리자에 안 보이던 문제** (`4e01743`) — 좌석 등급·객실 구성·
  출발 공항이 저장만 되고 고객 견적서에도 관리자 상세에도 표시되지 않았다.
  좌석 최대 4배·객실 최대 2배·출발지 16% 차이인데 문서에 근거가 없던 상태. 양쪽에 추가.

  ⚠ **공유 페이로드(script.js `shareData`)를 건드릴 때는 비공개 항목 유출을 조심할 것.**
  `rows: data.rows.filter(r => !r.muted)` 이 한 줄이 ENBT 수익·현지 수익금·여행자보험을
  고객에게서 가리는 유일한 방어선이다. `ai-loop/test_pG_quote_surfaces.js`가
  이 성질(+hiddenTotal·baseTotal·margin 미포함)을 회귀로 잡는다.

## ▶ 2026-07-29 — audit_rates '확인 대상' 값 확정 작업

**라운드 이탈 10건은 오류가 아니었다** (`462deef` — 감사기 수정)
- 이전 노트의 "과거 일괄조정(×1.35)이 일부에만 적용된 듯"이라는 추정은 **틀렸다.**
  그 10건의 최대공약수가 정확히 **1,350**이고 몫이 $150·$160·$35·$15·$274·$30 —
  현지 지상비를 USD로 받아 1,350원에 환산해 넣은 정상 입력이다.
  (×1.35는 1,350의 다른 표현이라 같은 숫자를 설명할 뿐이다.)
- audit_rates가 배율 추측보다 **최대공약수를 먼저** 보도록 고쳤다. gcd가 환율로 볼 만한
  범위(800~2,500)면 환산 흔적으로 보고한다.

  ⚠ **여기서 나온 진짜 숙제**: 환산 흔적이면 확인할 것은 값이 아니라 **통화 매핑**이다.
  다낭·나트랑·푸꾸옥은 `DEST_CURRENCY`가 VND, 캄보디아 KHR, 카자흐스탄 KZT인데
  원가는 USD로 고정돼 있다. 동이 20% 움직여도 실제 원가는 안 움직이고, 달러가 움직이면
  견적이 반응하지 않는다. **실제 정산 통화를 사용자가 확인 중 — 답 나오면 매핑 수정.**
  (사용자 답변: "잘 모르겠다, 일단 보류". 랜드사에 확인 필요.)

**하와이 관광비 100,000 → 200,000** (`36cd83f`)
- LA·SF와 나머지 8항목이 쌍둥이인데 관광비만 2배 벌어져 있었다. 낮은 쪽이 틀렸다고
  본 근거는 **하와이 자체 프로그램 구성** — b코스의 루아우 만찬·폴리네시안 문화센터·
  할레아칼라 일출 투어가 각 $70~180이라 셋만 합쳐도 100,000원을 넘는다.
  `sightseeing_fee`는 1인당 전체 일정 묶음이라(PC 확정) 원가가 안 나오는 값이었다.
- 실측: 하와이 5일 20명 4,289,866 → 4,389,316 (+2.3%). LA 무변경.

## ▶ 2026-07-29 후반 — 팀원 온보딩 대비 (다음 주부터 팀원 5명이 요율 단가 갱신)

역할 배분은 **매니저 1명 + 직원 4명**(사용자 결정). 직원도 개별 단가 수정은 가능하고,
일괄 조정·목적지 추가만 매니저 이상이다.

**감사기가 정적표를 보고 있던 문제** (`9ae8843`)
- 관리자 화면 수정분은 DB(rate_overrides)에 저장되고 고객 견적서는 그 값을 쓰는데,
  `audit_rates.js`는 data.js만 읽어 **아무도 팔지 않는 가격을 검사**하고 있었다.
  발견 시점에 이미 일본 6곳 항공료가 어긋나 있었다(도쿄 표 380,000 / 실제 399,000).
- `ai-loop/live_rates.js` 신설. `/api/rates` GET(공개)에서 오버라이드·커스텀 목적지를
  받아 정적표 위에 병합한다. 병합 순서는 script.js `applyRateOverrides()`와 동일
  (커스텀 편입 → 오버라이드). **기본이 라이브**, `--static`으로 정적값 강제.
  네트워크 실패 시 정적값으로 내려가되 그 사실을 화면에 찍는다.

  ~~⚠ audit_consistency.js는 아직 정적값만 본다.~~ → **2026-07-30 PQ에서 해결.**
  라이브가 기본이고 커스텀 목적지 전용 절이 생겼다. INSURANCE_ZONES 편입은 PP,
  DEST_SEASON_PROFILES 편입은 PQ에서 연결 완료.

**요율 오타 서버 상한** (`1f7b932`)
- 서버 검증이 "숫자이고 0 이상"뿐이라 0을 하나 더 붙여도 저장됐다. 화면의 3배
  confirm은 "예"를 누르면 그만이고, 화면을 안 거치는 요청은 아예 통과했다.
- `FIELD_MAX` = **현행 요율표 최댓값의 약 5배**로 통일. 처음에 눈대중 7~10배를 넣었다가
  새 테스트가 식비·관광비·마진에서 10배 오타가 통과하는 걸 잡아냈다 —
  **상한을 넉넉히 잡을수록 정작 막아야 할 오타가 빠져나간다.** 상한을 올릴 일이
  생겨도 이 5배 규칙을 유지할 것.
- 상한 위반은 조용히 버리지 않고 400 + 어느 항목이 얼마나 넘었는지 반환
  (`findOutOfRange`). 기존처럼 필터링해 버리면 "저장됨" 응답이 오고 팀원은 반영된 줄 안다.
- admin.html `rateSaveErrorMessage`로 에러 코드를 사람 말로. 요율 PATCH 6곳 전부 연결.
- 일괄 조정·되돌리기의 **부분 실패**를 드러내게 했다. 예전엔 "3/16 적용"만 띄우고
  실패를 삼켜서, 권역 안에서 가격이 어긋난 채 남아도 알 수 없었다.
- `ai-loop/test_pH_rate_guard.js` 65 pass. 현행 값 통과와 10배 오타 차단을 양쪽에서
  검사하므로 상한을 한쪽으로 치우치게 고치면 잡힌다. 전체 439 pass / 0 fail.

**팀원용 가이드**: Claude Artifact로 발행(사장님 계정에서 공유 가능).
로그인·편집 절차·되돌리기·에러 메시지·상태 배지·권한표·계정 생성 절차 포함.

**직원 자가 가입 — 가입코드 + 승인** (`aa737af`, 운영 마이그레이션·배포 완료)
- 사장님이 계정을 손으로 만드는 대신 직원이 로그인 화면에서 직접 신청하고 승인받는 흐름
  (사용자 결정). `/admin.html`이 로그인 없이 열리는 주소라 두 겹으로 막았다:
  ① **가입코드** — app_settings의 `staff_signup_code`. 없으면 가입 기능 자체가 꺼짐(기본값).
     관리자 화면에서 설정/해제. 사장님이 직원에게 불러줘야 해서 평문 저장(초대 코드지
     비밀번호가 아니고, 유출돼도 승인 단계가 남는다). 비교는 timingSafeEqual.
  ② **승인** — 신청은 항상 `role='staff', active=false` **리터럴**로만 생성. 요청 본문의
     role/active는 읽지 않는다. **여기서 권한 상승이 되면 가입코드 하나로 관리자 계정이
     만들어진다.** login.js가 !active를 거부해 승인 전 로그인 차단.
- **`staff_accounts.self_signup` 컬럼 신설** — active만으로는 '승인 대기'와 '관리자가
  꺼둔 계정'이 구분 안 돼 대기자를 놓치거나 내보낸 계정을 되살리게 된다. 승인하면 내려감.
- **거절은 전용 액션(`rejectSignup`)** — updateStaff로 흉내내면 '활성화 후 비활성화'가 되어
  그 사이 로그인이 열린다. active=false + self_signup=false를 한 번에.
- 대기열 상한 20건(코드 유출 시 DB 오염 차단). `self_signup`만 세므로 퇴사자 비활성화는 무관.
- `ai-loop/test_pI_staff_signup.js` 41 pass — 위 성질을 원문 대조로 고정. 전체 480 pass.
- 프로덕션 확인: 가입 403 `signup_disabled`(기본 꺼짐), 관리자 액션 6종 전부 401.

**미완**: 사장님이 가입코드를 정하고(설정 → 직원 계정 관리) 팀원에게 알려준 뒤,
올라온 신청을 승인 → 팀장 1명만 매니저로 승급. 다 들어오면 코드를 비워 가입을 닫을 것.

## ▶ 2026-07-29 심층 점검 — 같은 유형의 오류 전수 조사 (전부 라이브)

"안전망이 진실을 안 본다 / 검증이 없다 / 실패가 조용하다 / 권한이 없다"는 축으로
코드 전체를 훑어 4건을 찾았다. **관리자 화면 저장형 XSS가 가장 심각했다.**

**① 관리자 화면 저장형 XSS** (`42a5a6f`) — 치명
- `/api/quotes`·`/api/inquiries` POST는 **인증 없이 누구나** 호출하는데 본문의 `id`를
  검증 없이 저장했고(`payload.id || 생성`), 관리자 화면은 그 id를
  `onclick="openDetail('${c.id}')"`에 이스케이프 없이 넣었다.
  → 익명 제출자의 문자열이 **로그인한 관리자 세션에서 실행**된다. 쿠키가 HttpOnly라
  값은 못 훔치지만 코드가 관리자 오리진에서 도니 쿠키는 자동으로 붙는다 —
  직원 계정 생성·요율 변조·고객정보 유출까지 가능. owner가 열면 완전 탈취.
- jsdom 재현 확인. **운영 DB 26건 전부 정상 형식, 악용 흔적 0건**(열리기 전에 막음).
- ⚠ **이 자리는 esc()로 못 막는다** — esc()는 홑따옴표를 안 바꾸고, `&#39;`로 바꿔도
  HTML 파서가 `'`로 되돌린 뒤 JS가 해석해 그대로 탈출한다. 그래서 이스케이프가 아니라
  **안전한 문자만 남기기**(`safeId`)로 갔다. id는 원래 영숫자뿐이라 정상값은 무영향.
- 서버 `api/_lib/public_input.js` 신설(safeId·크기상한 64KB·숫자 강제), 화면 `safeId()`
  헬퍼로 id 보간 8곳 전부 경유. **id를 템플릿에 넣을 땐 반드시 safeId를 거칠 것.**
- 형식 위반 시 400으로 거절하지 않고 **id만 교체**한다 — 거절하면 고객 리드가 유실된다.

**② 요율 동시 편집 시 변경이 조용히 사라짐** (`0213fa4`)
- 기존 PATCH는 select → JS 병합 → 통째 덮어쓰기(read-modify-write). 두 사람이 같은
  목적지를 동시에 저장하면 나중 사람이 낡은 전체 값으로 덮어써 먼저 저장한 변경이 사라진다.
  **둘 다 "저장되었습니다"를 받고 변경 이력에도 두 건 다 남아서 아무도 눈치채지 못한다.**
  1명일 땐 안 보이다 5명이 되면 반드시 밟는 종류.
- 병합을 SQL 안으로: `overrides = coalesce(...) || excluded.overrides` + `returning`.
  다른 공유 데이터(콘텐츠 키단위 upsert, 진행기록 `activity_log ||`)는 이미 안전했다.

**③ 문의·견적 개별 삭제에 권한 제한 없음** (`0213fa4`)
- 로그인만 하면 누구나 고객 리드를 영구 삭제 가능했다(되돌리기·기록 없음).
  목적지 삭제·일괄조정은 이미 매니저 이상이라 설계와도 어긋났다. → **매니저 이상**으로
  통일(사용자 확인). 서버·화면 양쪽에서 막는다.

**④ 고객 견적서 화면 일차 번호 주입** (`175f7a5`)
- `estimate-view.html`이 공유 payload의 `day.day`를 `data-day`·`switchDayTab()`에
  날것으로 넣었다. 공유 payload는 인증 없이 만들 수 있고, **담당자가 고객 공유 링크를
  열어보는 건 일상 동작**이라 그 세션에서 코드가 도는 경로가 된다. → `Number()` 강제.

검증: `test_pJ_public_input.js`(44) + `test_pK_concurrency_perms.js`(18) 신설.
test_pJ는 jsdom으로 공격 문자열 5종을 **실제 렌더**해 핸들러가 깨지는지 본다.
전체 542 pass / 0 fail.

⚠ **앞으로 화면에 값을 넣을 때 기준**: 공개 POST로 들어온 값(문의·견적·공유 payload)은
전부 신뢰 불가다. 문자열은 `esc()`, id는 `safeId()`, 숫자 자리는 `Number()`.
인라인 `onclick` 안에 문자열을 넣는 구조 자체를 피하는 게 가장 안전하다.

## ▶ 2026-07-29 GPT 협의 점검 (PM·PN) — 전부 라이브

GPT 3라운드 + 자체 퍼징. **GPT 1라운드 6건 중 5건은 코드·실측으로 기각**됐다
(누진공제식 tieredTotal, KST에서 정상인 리드타임, 이미 처리된 해넘이 구간 등).
근거 전문은 `ai-loop/pL_prompt_round1~3.txt` / `pL_gpt_round1~3.txt`.

**신규 도구: `ai-loop/fuzz_invariants.js`** — 견적 엔진 불변식 퍼저.
목적지 55 × 인원 17 × 일수 14 × 출발일 4 = **52,360건**에서 NaN/음수/행합계불일치/
1인당불일치/일수 역전/노출총액 초과/인원 절벽(+15%↑)을 검사. `--full`로 전체,
기본은 샘플(3,080건). **계산식을 바꿨으면 회귀 스위트와 함께 이것도 돌릴 것.**

**PM 음력 연휴가 엉뚱한 날에 붙고 있었다** (`5e83049`) — 실제로 틀린 금액이 나가던 건
- `PEAK_CALENDAR`가 설·추석·춘절을 'MM-DD'로 **매년 반복**시키는데 값은 2027년 기준.
  즉 2027년 말고는 어긋났다. 실측(방콕 20명 5일 1인당):
    2026-09-15(평범한 날) 1,857,077 → 1,648,269 (**-11.2%** 과청구 해소)
    2026-09-25(실제 추석) 1,648,269 → 1,857,077 (**+12.7%** 놓치던 피크)
    2026-02-17(실제 설)   1,961,078 → 2,271,161 (+15.8%) · 상해 춘절 +15.5%
  오늘 기준 2026년 9월 출발은 7주 앞이라 **라이브에서 틀린 금액이 나가던 중이었다.**
- ⚠ **당시 주석이 "좁은 구간은 피크를 놓칠 뿐이라 안전"이라고 단정하고 있었다 — 틀린
  논리다.** 좁아도 어긋난 해에는 평범한 날 위에 얹힌다. 이런 주석이 다음 사람을
  멈춰 세운다. 반례를 주석에 박아뒀다.
- `LUNAR_PEAKS`(연도별 절대 날짜) 신설, 2026~2028 등록. 날짜는 사장님 확인 완료.
  **등록 안 된 연도는 계수 1.0** — 근사로 때우지 않는다(견적서에 근거 없는 "설 연휴"
  할증이 붙는 건 신뢰 문제).
- 연도별 테이블의 약점은 등록이 끝나면 조용히 피크가 사라지는 것 →
  `test_pM_lunar_peaks.js`가 **커버리지 12개월 미만이면 실패**한다. 빨간불이면
  script.js `LUNAR_PEAKS`에 다음 해 3줄 추가. 현재 2028-10-05까지(약 26개월).
- 곁다리: `bizCount`가 "2.5" 입력을 받아 가중평균에 2.5명이 들어가던 것 → floor + step="1".

**PN 환율의 조용한 지점 2곳** (`895c618`) — 계산식 불변, 보이게만 함
- ±30% 클램프에 걸린 사실이 어디에도 안 보였다. 걸리면 환율 변동이 견적에 덜 반영되는
  것이라 안전장치가 아니라 방치 상태다 → 엔진 `FX_CLAMPED` 기록 + 요율 표 🚨환율상한 배지.
  ⚠ **admin.html은 script.js를 로드하지 않아** 임계값이 양쪽에 따로 있다(0.7/1.3 ↔ 30%).
  `test_pN_fx_guards.js`가 두 값의 일치를 대조한다.
- "확인함" 버튼이 **환율 기준점까지 재설정**하는데 안내문에 그 말이 없었다. 담당자는
  "가격 그대로"만 보고 누르지만 쌓인 환율 보정이 사라진다. 어느 쪽이 맞는지는 랜드사에서
  확인한 게 '원화 금액'인지 '현지 가격'인지에 달려 시스템이 알 수 없다 → **동작은 그대로
  두고** 현재 몇 %가 걸려 있는지와 판단 기준을 confirm에 띄운다.
- 운영 실측: 55곳 전부 기준일 7/20, 최대 편차 -2.8%, 클램프 0곳.

**PO 서버 검증 통과분만 링크 발급 + ?d= 제거** (`bfb3ef5`) — 위 '미해결'을 해결한 것
- 예전엔 견적 내용을 base64로 URL에 담은 `?d=` 링크를 estimate-view.html이 그대로
  렌더했다. 서버를 안 거치니 발급 기록조차 없어 **누구든 우리 도메인에 임의 금액의
  '비즈페이지 견적서'를 만들 수 있었다.** 가격 분쟁에서 우리 도메인이 상대 주장의
  근거가 되는 구조였다.
- **고객 버튼은 그대로 두고 내용의 출처만 서버로 옮겼다**(사용자와 협의해 결정).
  MICE는 실무자가 결정권자에게 보여주는 동선이 핵심이라 링크를 뺏으면 전환 손해가
  크고, 위조는 '누가 누르는가'가 아니라 '내용을 누가 만드는가'의 문제이기 때문.
- **재계산이 아니라 대조**다. 엔진 5,000줄을 서버로 옮기지 않고, payload의 P6
  스냅샷(입력 전체 + 적용 계수 전체 + 항목별 단가·수량)을 권위 요율표·계수와 8단계로
  대조한다. 공식을 복제하면 두 벌이 어긋나는 문제가 생기는데 이 프로젝트가 이미
  여러 번 겪은 유형이라 피했다.
  ⚠ **한계**: 계수와 금액을 앞뒤 맞게 바꾼 미세 조작(±수%)은 못 잡는다. 잡으려면
  엔진을 서버로 옮겨야 한다. 그래서 결과를 ok/fail이 아니라 **단계별 기록**으로 남긴다.
- 검증은 두 지점: `/api/quotes` 저장 시(기록만, 저장은 함) · `/api/quote-shares`
  발급 시(게이트). **실패해도 견적은 저장한다** — verdict가 'reject'가 아니라 'review'인
  이유다. 견적은 상담의 시작점이라 리드 유실이 훨씬 큰 손해다.
- 고객 화면: 공유 모달이 검증 중 → 발급 완료 / 담당자 확인 3상태. 진행 문구는 서버가
  실제로 하는 검사 이름과 같게 썼다(없는 절차를 있는 척하지 않기 위해).
- 담당자 발급: 관리자 → 견적 관리 상세 → 🔗 견적서 링크 발급. 검증에 걸려도 사람
  판단으로 발급 가능(걸린 항목을 먼저 보여주고 확인). 이력은 진행 기록에 남는다.
- ⚠ `estimate-view.html`의 `decodeShareData`를 되살리면 위조 경로가 그대로 다시 열린다.

**PP 검증 결과 가시화 + 커스텀 목적지 보험 권역** (`b104a57`, 마이그레이션·배포 완료)
- **PO에서 내가 만든 구멍을 메운 것** — `_verify`를 저장만 하고 관리자 화면에 안 띄워서
  아무도 못 보는 기록이었다. 이번 점검 내내 지적한 유형을 그대로 반복했다.
  → 견적 목록에 ⚠ 배지(통과 건에는 안 띄운다 — 정상이 대부분이라 소음이 된다),
    상세에 걸린 단계를 사람 말로.
- **커스텀 목적지가 `INSURANCE_ZONES`에 편입되지 않았다.** script.js는 destinationRates·
  BIZ_ZONES·남반구 목록·select에는 넣는데 보험 권역만 빠뜨렸다. getInsuranceZone이
  못 찾아 **계수 1.00(중립) 폴백** — 권역별 0.85~1.80이라 최대 80% 어긋나는데 콘솔
  경고만 남았다. 지금 커스텀 목적지 0건이라 안 터졌을 뿐, 매니저가 하나 추가하는
  순간 조용히 틀린 보험료가 나간다.
  → DB 컬럼(`insurance_zone`) → 폼 선택칸 → 서버 검증 → GET 응답 → 엔진 편입, 다섯 곳 연결.
  기본값 asiaMid는 계수 1.00이라 **옛 폴백과 동작이 같다**(마이그레이션만으로 금액 불변).
  ⚠ **허용 키 목록이 서버·엔진·폼 세 곳에 있다.** 하나만 늘리면 저장은 되는데 엔진이
  못 찾아 중립값으로 떨어진다 → `test_pP_custom_dest.js`가 세 목록을 대조한다.
  (PQ의 시즌 프로파일은 이 부담을 피해 data.js 한 곳에서 서버·폼이 파생되게 했다.
  **커스텀 목적지에 새 분류값을 추가할 때는 PQ 방식을 따를 것** — 목록을 세 번 적지 말고
  data.js를 진실로 두고 서버·폼이 그걸 읽게 한다.)

## ▶ 남은 아이디어 (미검증 — 착수 전 사용자 확인 필요)

- **사용자 단가 회신 대기 2건** (사용자가 "실제 견적서 단가로 지정" 선택 — 숫자만 오면 반영):
  - 발리 가이드비 99,000 — 동남아 최저가(다음으로 싼 다낭 202,500의 절반)인데
    발리 식비 40,000은 아시아 전체 최고가(일본·싱가포르 25,000보다 높음)라 서로 안 맞음.
    발리 현지 랜드사 견적서의 가이드 일당 확인 후 반영.
  - 괌 가이드비 405,000 vs 사이판 210,000 — 나머지 8항목은 쌍둥이인데 가이드만 1.93배.
    괌 405,000은 시드니·독일·밴쿠버(400,000)보다도 비싸다. 양쪽 실제 일당 확인 후 반영.
- **동남아·중앙아 정산 통화** — 위 ⚠ 항목. 랜드사 확인 후 `dest_currency.js` 수정.
- ~~리드 유실 방지~~ → **2026-07-30 PR(고객)·PV(관리자)로 완료**. 위 절 참고.
- ~~비활성화한 계정이 최대 12시간 더 살아 있다~~ → **2026-07-30 PW로 완료**(배포 대기).
  사장님이 '요청마다 DB 확인'(선택지 ①)을 골랐다. 위 PW 절 참고.
- **`admin-quote.html`(내부 견적 작성 도구)은 아직 미점검** — PR 절의 '알려진 한계'
  그대로다. PV와 같은 유형(저장 실패를 담당자에게 안 띄움)이 남아 있을 자리.
- **비정확도 영역** — 이미지 폴더 빈약 / Pexels 키.
- **결함 생성기 ①(목록 산포)의 구조적 제거** — PQ에서 시즌 프로파일만 data.js 파생으로
  바꿨다. 보험 권역·좌석 구간·통화·지역은 아직 서버·엔진·폼에 따로 적혀 있다(테스트가
  대조하고 있어 당장 위험하진 않지만, 같은 유형이 또 나올 자리다). 다음 후보 1순위.
- 모델 상수 기반 정확도 개선은 **수확 체감 구간**. 더 큰 향상은 실측 데이터 필요.

## ▶ 반드시 지킬 작업 규칙 (과거 사고·합의에서 나온 것)

- **index.html / styles.css / script.js는 절대 전체 재작성 금지** — 섹션 단위 Edit만.
  (과거 전체 재작성이 토큰 한도로 잘려 스타일 통째 유실 사고 있었음.)
- **배포 = `git push origin master`** (프로덕션 자동배포). 계산식 바꾸는 변경은 배포 전
  사용자 승인. additive·계산식 불변 변경은 검증 후 바로 push 가능(정책 합의됨).
- **검증 방법(jsdom)**: `npm install --no-save jsdom` 돼 있음(node_modules는 gitignore).
  테스트 실행 시 `NODE_PATH="<프로젝트경로>/node_modules"` 필요. data.js + dest_currency.js
  + script.js를 **하나의 문자열로 합쳐 한 번에 eval**해야 const 전역이 서로 보임.
  기존 테스트는 `ai-loop/test_p*.js` — 계산식을 바꿨으면 **전부 한 번 돌려볼 것**
  (P13 때 P10 테스트가 낡아 깨진 채 방치된 전례 있음. PB에서 갱신 완료).
- **Vercel Hobby = 서버리스 함수 12개 제한 이미 도달** — 새 API는 새 파일 말고 기존
  파일에 `?action=` 분기로 추가.
- **실측 데이터가 프로덕션 DB에 0건** — 측정기반 작업(P5 신뢰구간 실동작·P6 역검증·
  실측 튜닝)은 계약완료 견적이 쌓여야 의미 있음. 지금은 보류.
- **확정 우선순위 없이 바로 코딩하지 말 것** — 후보 제시하고 사용자 확인 먼저.

## ▶ 미완 롤아웃 (사용자가 실행해야 하는 것, 코드는 완료)

- **팀원 5명 온보딩** — 계정 생성 방식이 자가 가입으로 바뀌었다(2026-07-29).
  사장님이 할 일: ①설정 → 직원 계정 관리에서 **가입코드 설정** ②팀원에게 코드 전달
  ③올라온 신청 **승인** ④팀장 1명만 **매니저**로 승급 ⑤다 들어오면 **코드 비워 가입 닫기**.
  (2026-07-29 기준 운영 DB 계정 3건 전부 활성, 승인 대기 0건, 가입코드 미설정=가입 꺼짐)
- 팀원용 사용 가이드는 Claude Artifact로 발행돼 있다(사장님 계정에서 공유).
  요율 편집 절차·되돌리기·에러 메시지·상태 배지·권한표·가입 절차 포함.

---
※ 작업이 일단락되면 이 파일은 지워도 됨(일회성 인수인계용).
